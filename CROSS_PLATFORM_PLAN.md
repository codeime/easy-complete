# Easy Complete 跨平台改造计划

**分支:** `feat/cross-platform`（执行指针：`fix/cross-platform-audit-1`，见 §8）
**日期:** 2026-08-20
**状态:** 已评估、已修订。macOS 产品面保持唯一发运目标，直到本计划对应里程碑合入并有 CI 绿灯。
**依据:** 仓库现状 + 交叉编译实测 + 深度调研报告（Partial）。本文件取代口头推断。

---

## 0. 一句话结论

当前**唯一能编、能装、能发**的产品面是 **Apple Silicon macOS**。树里的 Linux / Windows 桌面后端是 GPUI 迁移前就坏掉的 fork 考古，**不能当现成实现，也不能靠切 `cfg` 复活**。跨平台必须拆成：

1. **已经可复用的底座**（IR 引擎、Unix IPC、POSIX PTY、shell hook）
2. **必须按操作系统重写的原生表面**（浮层宿主、caret、权限、托盘、开机启动、打包）

顺序是 **Linux 先行、Windows 后置**。Linux 先走通「无头引擎 → `ecterm` + shell hook → GPUI 浮层」，Windows 再补 named pipe 与 ConPTY。

---

## 1. 目标与非目标

### 1.1 产品目标（按里程碑）

| 里程碑 | 用户能得到什么 | 平台 |
| --- | --- | --- |
| **M0** | 本计划落地；非 macOS 编译失败变成 CI 红灯而不是口头结论 | 仓库 / CI |
| **M1** | `ec engine complete --buffer "git ch"` 在 Linux 上跑通（无浮层） | Linux |
| **M2** | `ecterm` + zsh/bash/fish hook 在 Linux 上驱动引擎（仍无浮层，结果可走 stdout / 日志） | Linux |
| **M3** | GPUI 补全列表跟随 caret；caret 缺失则隐藏，**不用窗口矩形兜底** | Linux（X11 + GNOME 优先） |
| **M4** | Linux 安装物（`.desktop` + 数据目录 + `specs-ir`），可卸载 | Linux |
| **M5** | Windows 上 IPC + ConPTY + 无头引擎 | Windows |
| **M6** | Windows GPUI 浮层 + 安装物 | Windows |

macOS 行为、Accessibility、IME、`.app` / DMG **全程不得回退**。每一步的回归门槛写在第 7 节。

### 1.2 非目标（写死，review 时用来否决跑偏）

- **不**重新引入 `wry` / WKWebView / `packages/autocomplete-app` / `packages/dashboard-app`。
- **不**在 caret 缺失时用窗口矩形摆放列表（比没有更糟）。`overlay.rs` 已写明：没收到 caret 就停在上次有效位置或保持隐藏。
- **不**把已删除的 `crates/fig_desktop/src/platform/{linux,windows.rs}` 考古树恢复进仓库；Linux/Windows 表面对着现行 `event.rs` / `overlay.rs` / GPUI 宿主**重写**，不要翻 `cfg` 复活。
- **不**从 v2.0.45 删掉的 universal / Linux / Windows Makefile 或 Linux 打包资源「恢复发行」。
- **不**把 IME / IBus / UI Automation 当成 v1 的硬依赖。Linux / Windows v1 先走 **PTY + 编辑缓冲**；caret 是浮层的前置，不是引擎的前置。
- **不**为了重连而去 disable/enable 输入源（macOS IME 的已知坑；别复制到别的平台）。
- **不**把 AppKit / `objc2-app-kit` / `macos-utils` 链进 `fig_util`（会拖进每个 `ecterm` 标签页）。
- **不**在本计划里重做 Intel Mac / universal 二进制。
- **不**把 Sway / 任意 Wayland compositor 列入 v1。
- **不**把 `scripts/setup.sh` 里的 WebKit/GTK/IBus 依赖表当成现行桌面构建的真源。

---

## 2. 现状（经核对）

> **快照日期 2026-08-20。** 下面 2.1–2.4 里「编不过」的条目已在后续 PR 关掉；现行门禁、cfg 与执行指针以 **§8** 为准。

### 2.1 发运与 CI

- 自 **v2.0.45** 起，发行管线端到端改为 ARM64-only，并删除过时的 universal / Linux / Windows Makefile 与 Linux 打包资源。
- `scripts/build-app.sh` 在非 Darwin/arm64 上直接退出。唯一发运物是 `.app`：`easy-complete`、`ec`、`ecterm`、`Contents/Helpers/EasyCompleteInputMethod.app`。
- `install.sh` / `uninstall.sh` / `make-dmg.sh` / `sign-macos-app.sh` / `notarize-dmg.sh` 只围绕该 bundle。
- `.github/workflows/ci.yml`：JavaScript 在 `ubuntu-latest`。Rust：`macos-15` 工作区 clippy/test + dist 发运二进制；`rust-linux`（ubuntu-latest）与 `rust-windows`（windows-latest）对同一 crate 列表做 clippy `-D warnings` + test + 无头 `ec engine complete`（含 `fig_desktop` / `ec_gpui`）。两 job **不是**桌面会话：无 live X11/IBus/HWND/ConPTY I/O，也不是发运。Release 只打 ARM DMG。
- README 明确：**Platform: macOS only. The published DMG is Apple Silicon / ARM64 only.**

### 2.2 必须按 OS 重写的表面

| 表面 | 现行实现 | 跨平台含义 |
| --- | --- | --- |
| 浮层宿主 | `gpui_host.rs`：一个 `NSApplication` / `GPUIApplication` 托管 overlay + settings + tray | 抽出与 AppKit 无关的事件泵；macOS 保留 `ensure_gpui_ns_application` |
| 浮层窗口 | `ec_gpui/src/macos.rs`：`orderOut` / `orderFrontRegardless` / `setLevel` / 非激活 panel / join-all-spaces | Linux/Windows 各自实现「不抢焦点、始终在终端之上、隐藏时停车」 |
| Caret 坐标 | Quartz 顶左 → Cocoa，原点必须是 `NSScreen.screens[0]`，禁止 `mainScreen` | 统一「屏幕空间 caret + origin」；每 OS 自己填，转换错误会把列表打到另一块屏 |
| 权限 | Accessibility：`AXIsProcessTrusted*`、`AXUIElement`、`AXObserver`；安装时二进制哈希变了才 `tccutil reset` | Linux：AT-SPI / 合成器协议（v1 能避开则避开）。Windows：UIA 权限模型 |
| IME | `fig_input_method` + `ec_hitoolbox`（TIS / IMKit / HIToolbox 两个列表） | **Linux/Windows v1 不做对等 IME**。Ghostty/Kitty 类终端在那些平台另开 caret 通道 |
| 开机启动 | macOS 13+ `SMAppService`；12 为 LaunchAgent | Linux：XDG Autostart（`fig_integrations::desktop_entry` 已有草稿）。Windows：以后再说 |
| 打包 | Darwin arm64 `.app` + Sparkle | Linux：`.desktop` + 前缀布局，不是 App 包。Windows：zip/MSI，不是 DMG |

`ec_gpui`：macOS 窗口模块已 `cfg(target_os = "macos")`。Linux 走 `linux.rs`（按标题 find + X11 configure/map），Windows 走 `windows.rs`（GPUI HWND + `SetWindowPos`）；HWND/标志策略在 `windows_overlay.rs`，每 OS 可单测。考古 `platform/linux/` 与 `platform/windows.rs` **已删除，勿恢复**。

### 2.3 已可复用的底座

| 组件 | 证据 | 限制 |
| --- | --- | --- |
| `ec_engine` | IR + QuickJS hook，不经桌面进程；`ec engine complete` | `process.rs` 进程组、`filegen.rs` `getpwnam_r` 是 unix cfg；Windows 要补 |
| `fig_os_shim` | 对 Mac/Linux/Windows 建模；**本机交叉编译 `aarch64-unknown-linux-gnu` 已通过** | 只是 I/O 门面，不是桌面 |
| `fig_proto` | 依赖 `fig_util`，所以被其 Linux 编译错误挡住 | 协议本身与 OS 无关 |
| `fig_settings` | rusqlite bundled | 交叉编译需要目标 gcc；Linux CI 原生即可 |
| `fig_ipc` | Unix socket（macOS/Linux）；Windows named pipe（`windows_pipe.rs`，slug 在 `pipe_name.rs`） | accept/connect 测试是 `cfg(windows)`；retry/bind 策略每 OS 可测 |
| `figterm` PTY | macOS/Linux：`posix_openpt`；Windows：ConPTY 在 `pty/win/` | ConPTY 只在 `rust-windows` 编译；无 live I/O 测试 |
| shell hook | zsh/bash/fish rc 注入（`fig_integrations`） | `ec init` 在桌面 app 未运行时整段站起，Linux 无桌面时要想清楚 |
| `fig_integrations::desktop_entry` | XDG desktop / autostart | 可用，但安装路径仍假设旧 Fig 布局 |

### 2.4 编不过的残留（2026-08-20 实测；后续 PR 已关，见 §8）

本机 `cargo check --target aarch64-unknown-linux-gnu`：

- `fig_os_shim`：**通过**。
- `fig_util`：**失败**。
  - `directories.rs`：`use crate::linux::PACKAGE_NAME` —— 没有 `fig_util::linux` 模块。
  - `crate::consts::linux::DESKTOP_APP_WM_CLASS` —— `consts.rs` 只有 `macos`，没有 `linux`。
  - `APP_PROCESS_NAME` 只在 `macos` / `windows` 下定义，Linux 桌面入口会再踩一次。
- `fig_settings` / `ec_engine`：交叉编译卡在 `libsqlite3-sys` 找不到 `aarch64-linux-gnu-gcc`。这**不能**当成源码错误；验证必须放到 **ubuntu-latest 原生** job。

读码确认、尚未在 Linux 原生 runner 上编（考古 `platform/linux/` 与 `platform/windows.rs` **已删除，勿恢复**）：

| 位置 | 问题 |
| --- | --- |
| `fig_install` / `fig_integrations` / `ec_cli` 的 GNOME 路径 | 调用不存在的 crate `dbus::gnome_shell`（工作区里没有 `dbus` 包） |
| `fig_install/src/linux.rs` | 还用未声明的 `tar` / `zstd` |
| `ec_cli/Cargo.toml` | **无条件**依赖 `appkit-nsworkspace-bindings`，源码里 **零引用** |
| `ec_gpui` | `mod macos` 无条件 |
| `fig_input_method` / `ec_hitoolbox` | 非 macOS 可编，但是打印失败退出或空操作 |
| `scripts/setup.sh` | 编译依赖对齐 `rust-linux`：GTK 托盘、X11、Vulkan **头文件**。不装 WebKit。运行时浮层要 Vulkan ICD + X11；caret 要 IBus/AT-SPI（不写进 apt 列表） |

**Linux IBus：** 活路是 `platform/linux_caret/ibus.rs`。WebView 时代的 `platform/linux/ibus.rs` 草稿**已删除，勿恢复**。

---

## 3. 架构原则（每步 review 对照）

```
                 ┌─────────────────────────────────────┐
                 │  ec_engine  (IR + QuickJS, OS-agnostic) │
                 └─────────────────────────────────────┘
                                    ▲
           fig_ipc transport        │ CompleteRequest
     Unix socket (macOS/Linux)      │
     named pipe  (Windows, 后置)    │
                 ┌─────────────────────────────────────┐
                 │  ecterm (PTY + 编辑缓冲)              │
                 │  POSIX pty | ConPTY                   │
                 └─────────────────────────────────────┘
                                    ▲
                 ┌─────────────────────────────────────┐
                 │  OverlayHost (GPUI, 无 WebView)       │
                 │  OverlayPlatform trait:               │
                 │   - caret → 屏幕坐标                    │
                 │   - show / park（不抢焦点）              │
                 │   - 无 caret ⇒ 隐藏                    │
                 └─────────────────────────────────────┘
                    macOS │ Linux │ Windows
                    AppKit│ X11/  │ Win32/
                          │ layer │ UIA
```

1. **底座先绿，表面后写。** 先让无头 crate 在目标 OS 的 CI 上 `clippy -D warnings`，再碰 GPUI。
2. **一个 caret 契约。** 浮层只吃 `WindowPosition::RelativeToCaret { caret_position, caret_size, origin }`。新后端必须发这个，禁止再引入 `PositionRelativeToRect`。
3. **macOS 实现继续住在 `cfg(target_os = "macos")` 后面**，不要为了跨平台把 AppKit 调用摊进共用模块。
4. **`fig_desktop` 的 linux/windows 目录要么重写要么删掉**，不要「修到能选中」。
5. **进程内存约束仍然有效：** `ecterm` 按标签页翻倍；`fig_util` 禁止 AppKit；grid 的 `max_scroll_limit = 1` 不能改成 0。

---

## 4. 分步改造计划

每一步对应一个可单独 review 的 PR。合入条件见第 7 节。**不要跳步**：M3 之前没有 Linux 浮层，M1 之前没有 Linux CI。

### 阶段 A — 门禁与底座（M0 → M1）

#### PR-A1　补齐 `fig_util` 的 Linux 常量，让纯 Rust 底座在 Linux 目标上过编译

- **目标:** `fig_util` / `fig_os_shim` / `fig_proto` 在 Linux 上 `cargo check` 通过。
- **做:**
  - 在 `crates/fig_util/src/consts.rs` 增加 `linux` 模块：`PACKAGE_NAME`、`DESKTOP_APP_WM_CLASS`、`APP_PROCESS_NAME`（Linux 无 `.exe`）。
  - 修正 `directories.rs` 的 `crate::linux::PACKAGE_NAME` 引用（应走 `consts`，不要虚构 `fig_util::linux`）。
  - `resources_path()` 的 Linux 前缀用 `easy-complete`，不要沿用 `/usr/share/fig`。
- **不做:** 不定桌面后端；不改 macOS 路径；不引入 AppKit。
- **验收:**
  - 本机：`cargo check -p fig_util -p fig_os_shim -p fig_proto --target aarch64-unknown-linux-gnu`
  - macOS：`cargo test -p fig_util`、`cargo clippy --workspace -- -D warnings` 仍绿。
- **风险:** 常量字符串一旦被安装器/WM_CLASS 用上就难改；选 `easy-complete` / `dev.emmmm.easy-complete` 并在测试里钉死。

#### PR-A2　Linux CI：原生 runner 编无头 crate

- **目标:** 交叉编译缺 gcc **不再**被误当成源码结论。`fig_settings` + `ec_engine` 在 Ubuntu 上绿灯。
- **做:**
  - `.github/workflows/ci.yml` 增加 `rust-linux` job（`ubuntu-latest`）。
  - 第一刀只跑：`fig_os_shim`、`fig_util`、`fig_proto`、`fig_settings`、`ec_engine` 的 `clippy -D warnings` + `test`。
  - **不**在此 job 跑 `fig_desktop` / `ec_gpui` / `ec_cli`（现在必然红）。
- **不做:** 不把 Linux job 设成 `continue-on-error` 来掩盖失败。
- **验收:** PR 在 macos-15 **和** ubuntu 上都要绿；macOS dist 构建不受影响。
- **风险:** rusqlite bundled 在 Ubuntu 需要 `build-essential`；rquickjs 可能要 clang。写进 job 的 apt 列表，**不要**装 WebKit。

#### PR-A3　无头 CLI：`ec engine complete` 在 Linux 可用

- **目标:** 不启动桌面进程，Linux 上能对 `git ch` 给出补全。
- **做:**
  - `ec_cli`：把 `appkit-nsworkspace-bindings` 挪到 `cfg(target_os = "macos")`（源码零引用，纯减负）。
  - 给 `ec_cli` / `fig_install` / `fig_integrations` 里 `dbus::gnome_shell` 的 Linux 分支加 `cfg` 或暂用 `unimplemented` **编译桩**，让 `ec` 二进制能链上；**不要**实现 GNOME 扩展。
  - 引擎 specs 目录：Linux 用 `EC_SPECS_DIR` 或 `/usr/share/easy-complete/specs-ir`，不要读 `.app/Contents/Resources`。
  - 确认 unix 进程组 / `getpwnam_r` 在 Linux 测试下行为正确。
- **不做:** 不安装器、不做浮层、不修 doctor 的 GNOME 检查（可标 skip）。
- **验收:** Ubuntu CI：`cargo run -p ec_cli -- engine complete --buffer "git ch"` 退出 0 且 stdout 含 `checkout` 类建议；macOS 同样命令不回归。

### 阶段 B — PTY 与 shell（M2）

#### PR-B1　`fig_ipc` + `figterm` 在 Linux CI 编译并测 POSIX PTY

- **目标:** 一个 `ecterm` 进程能起来、grid 仍是 `max_scroll_limit = 1`、socket 走 `$XDG_RUNTIME_DIR/ecrun/`。
- **做:**
  - Linux CI 扩大到 `fig_ipc`、`figterm`。
  - 核对 `directories.rs` 的 runtime/socket 路径在 Linux 测试里走 XDG，而不是 `$TMPDIR` 的 macOS 形状。
  - Tokio 池保持 2 worker（标签页会翻倍）。
- **不做:** 不改 scrollback；不把 Windows named pipe 提前做进来。
- **验收:** `cargo test -p figterm -p fig_ipc` 在 Ubuntu 绿；macOS figterm 测试绿。
- **风险:** `alacritty_terminal` vendored fork 的 Linux termios 路径可能有死代码。以实际编译日志为准，不要预加依赖。

#### PR-B2　Shell 集成在无桌面 / 有桌面两种模式下可预测

- **目标:** Linux 上 `ec integrations install` 能写 zsh/bash/fish rc；桌面未运行时的 `ec init` 策略明确。
- **做:**
  - 文档化：无桌面时是「完全站起」（与现 macOS 一致）还是允许无头 hook。**建议 v1 与 macOS 一致**，避免和发行版自带补全抢。
  - 不要把 IME 安装步骤搬到 Linux。
- **验收:** 集成测试或脚本：在临时 `HOME` 里安装 hook，rc 可撤销；doctor 在无 dbus 时不 panic。
- **风险:** Otty / VS Code Terminal Suggest 共存逻辑是 macOS 上的产品决策，Linux 照抄。

### 阶段 C — GPUI 宿主去 AppKit 化（M3 前置）

#### PR-C1　`ec_gpui`：把 macOS 窗口硬化收进 `cfg`，抽出平台无关的列表

- **目标:** `ec_gpui` 在 Linux 上至少能编过 **list + theme**；overlay 窗口 API 变成 trait / cfg 分发。
- **做:**
  - `lib.rs`：`#[cfg(target_os = "macos")] mod macos;`
  - `overlay.rs` 里 `harden` / `park` / `set_overlay_frame` 改为 `platform` 模块（macOS 走现实现，其它 OS 先 `todo` 或空实现，但要能 `cargo check`）。
  - 列表、匹配、主题保持平台无关（已是）。
- **不做:** 不在这一步实现 X11 窗口；不升级 gpui，除非 check 证明 0.2.2 在 Linux 编不过。
- **验收:** Ubuntu：`cargo check -p ec_gpui`；macOS：overlay 单测 + 手测光标跟随不回归。
- **风险:** **gpui 0.2.2 在 Linux/Windows 上能否做出不抢焦点的 popup 未验证。** 若 0.2.2 的 Linux 后端不可用，升级 gpui 是单独的高风险 PR（C1b），禁止夹带功能。

#### PR-C2　`fig_desktop` 宿主：事件泵与平台后端解绑

- **目标:** `gpui_host::start_application` 在非 macOS 也能 `Application::new().run`；`ensure_gpui_ns_application` 仍仅 macOS。
- **做:**
  - `PlatformBoundEvent` 里 macOS 独有字段保持 `cfg`。
  - **删除或隔离** 编不过的 `platform/linux/*` 与 `platform/windows.rs`，换成最小 stub：`accessibility_is_enabled → None`、`get_cursor_position → None`、caret 事件 no-op。
  - stub 必须遵守：无 caret ⇒ overlay 隐藏。
- **不做:** 不在 stub 里用窗口矩形；不接 IBus；不把 tao EventLoop 请回主循环（GPUI 已是宿主）。
- **验收:** Ubuntu：`cargo check -p fig_desktop`（允许 overlay 永远隐藏）。macOS clippy workspace 绿；手测设置窗口 / 托盘 / 浮层。
- **风险:** `tao` + `tray-icon` + `muda` 在 Linux 上会拉 GTK。需要明确系统依赖，且不能把 WebKit 带回来。

### 阶段 D — Linux caret 与浮层（M3）

#### PR-D1　Caret 源（X11 优先）

- **目标:** 在 X11 终端里拿到 caret 矩形，转换成 `RelativeToCaret`，喂给现行 `overlay.rs`。
- **做:**
  - 新模块（不要在旧 `x11.rs` 上打补丁凑合）：声明 `x11rb`，跟踪焦点窗口。
  - 能从 IBus `SetCursorLocation` 得到的 caret **可以**复用其几何换算（它已经是 `RelativeToCaret`），但连接代码重写，依赖走 `Cargo.toml` 声明的 `zbus`，不要复活失踪的 `dbus` crate。
  - 无 caret：调用现有 hide/park。
- **不做:** 不做 Sway；不做「用窗口框猜 caret」；GNOME Wayland 放到 D2。
- **验收:** 手测至少：GNOME 终端或 kitty（X11 会话）、外接屏不要错位（原点策略写进代码注释，对标 macOS `screens[0]` 的教训）。自动化：几何换算单测。

#### PR-D2　Wayland（GNOME）caret —— 仅在 D1 稳定之后

- **目标:** GNOME Wayland 下浮层可用。
- **做:** 评估 layer-shell + 终端专有协议；**GNOME Shell 扩展不是 v1 必选项**（旧路径依赖失踪的 `dbus` crate 和已删打包资源）。
- **不做:** 不为「所有 Wayland」承诺同一实现。
- **验收:** 一台 GNOME Wayland 机器上手测；caret 丢失时列表隐藏。

#### PR-D3　Linux 浮层窗口行为

- **目标:** 不抢焦点、终端全屏时仍可见或按策略隐藏、hide 时 park 而不是销毁（保持上次尺寸，对标 macOS `orderOut`）。
- **验收:** 与 macOS 相同的隐藏触发：整段删 token、粘贴 / 调历史 → `HIDDEN_UNTIL_KEYPRESS`；自己插入豁免。这些逻辑已在 `overlay.rs`，平台层不得绕过。

### 阶段 E — Linux 安装物（M4）

#### PR-E1　布局与集成

- 前缀：`/usr/bin/ec`、`/usr/bin/ecterm`、`/usr/bin/easy-complete`（或 `/usr/libexec`）、`/usr/share/easy-complete/specs-ir`。
- `.desktop` + 可选 XDG autostart（已有 `desktop_entry.rs`）。
- 卸载对称。不要 `tccutil`，不要 IME symlink。

#### PR-E2　打包

- 先 tar.gz / 自描述目录，再考虑 deb。
- **禁止**把 `setup.sh` 的 WebKit 依赖写进包装。
- `build-app.sh` 保持 Darwin-only；另开 `scripts/build-linux.sh`，不要把 `.app` 逻辑 `if linux`。

### 阶段 F — Windows（M5 → M6，后置）

排在 Linux M3 之后。原因：IPC 是 `compile_error!`、引擎 unix-only 路径、桌面后端类型已经对不上。

| PR | 内容 |
| --- | --- |
| F1 | `fig_ipc` named pipe transport；公共 trait，Unix 实现保持现状 |
| F2 | `ec_engine` Windows 进程终止 / 无 `getpwnam` 的 `~user` |
| F3 | `figterm` ConPTY 在 windows-latest CI 编译 |
| F4 | **重写** caret（UIA），对接 `RelativeToCaret`；**删除** `PositionRelativeToRect` 残留，而不是让它重新编译 |
| F5 | GPUI 浮层 + 不抢焦点 |
| F6 | 安装物（zip 先于 MSI） |

Windows CI 用 `windows-latest`，与 macos / ubuntu 并列，互不 `continue-on-error`。

---

## 5. 建议的文件落点（按阶段）

| 阶段 | 主要文件 |
| --- | --- |
| A1 | `crates/fig_util/src/consts.rs`, `directories.rs` |
| A2 | `.github/workflows/ci.yml` |
| A3 | `crates/ec_cli/Cargo.toml`, `crates/fig_install/src/linux.rs`, `crates/ec_engine/src/{worker,process,filegen}.rs` |
| B1 | `crates/fig_ipc/src/unix_socket.rs`, `crates/figterm/src/pty/unix.rs`, `crates/fig_util/src/directories.rs` |
| B2 | `crates/fig_integrations/src/**`, `crates/ec_cli/src/cli/integrations.rs` |
| C1 | `crates/ec_gpui/src/{lib,overlay,macos}.rs`（新增 `platform/`） |
| C2 | `crates/fig_desktop/src/{gpui_host,platform/mod.rs}`；隔离或删除 `platform/linux/*`, `platform/windows.rs` |
| D1–D3 | **新建** `crates/fig_desktop/src/platform/linux_caret/` 或同等；`overlay.rs` 只消费 caret |
| E | `scripts/build-linux.sh`（新）, `crates/fig_integrations/src/desktop_entry.rs` |
| F | `crates/fig_ipc` 新 transport, `crates/figterm/src/pty/win/`, 新 Windows caret 模块 |

---

## 6. 计划评估与修订记录

调研原稿建议「先让 `fig_desktop` 的 linux/windows 分支在现行 GPUI 事件模型下编过」。评估后**否决作为第一步**，原因：

1. **类型已经对不上。** Windows 后端引用的 `RelativeDirection` / `FigWindowMap` / `PositionRelativeToRect` 在现行 `event.rs` 里不存在。把它「编过」等于在过时 API 上施工，下一步还得拆掉。
2. **依赖不在清单里。** Linux 后端要的 `x11rb`/`zbus`/`dbus` 未声明，且工作区没有 `dbus` crate。补依赖让考古文件通过 clippy，会把死代码变成 CI 负担。
3. **底座自己就编不过。** 实测 `fig_util` 缺 Linux 常量，`fig_proto` 被它拖死。桌面前必须先修这个。
4. **IBus 草稿比调研说的新一点。** 它已发 `RelativeToCaret`，但仍然不是 GPUI 路径，也不能编译。修订：D1 可以**借鉴几何**，不可以「打开 cfg」。
5. **交叉编译不是证据。** sqlite 缺 `aarch64-linux-gnu-gcc` 只说明本机无交叉工具链。A2 必须用 Ubuntu 原生 job。
6. **Linux 先行是现写进本文件的决策**，不是仓库里已有的官方顺序。依据是 POSIX PTY + Unix socket 已存在，Windows 要先长 IPC。
7. **`setup.sh` 的 WebKit 包是错误路标。** 现行 UI 是 GPUI。E 阶段的系统依赖从 GPUI/GTK/X11 重新列，不从 setup.sh 抄。
8. **`local_webview_data_dir` 这种名字不带进 Linux 安装布局。** 那是 WebView 时代的数据目录。**已关（2026-08-23）：** 函数已删；Linux uninstall 只清 `fig_data_dir`（`easy-complete` 前缀），不再扫 `q-desktop` 一类 webview 旁路。

**仍开放、必须用后续 PR 关闭的问题：**

- ~~gpui 0.2.2 能否在 Linux 上承载不激活 popup~~ **已关（2026-08-23）。** `WindowKind::PopUp` + `_NET_WM_WINDOW_TYPE_NOTIFICATION` + `_NET_WM_STATE_ABOVE`（map 之后再发 `_NET_WM_STATE` ClientMessage）。本机 `DISPLAY=:2` xfwm4：`Fig Autocomplete` 是 NOTIFICATION/ABOVE，`_NET_ACTIVE_WINDOW` 不是它。不必为这个升级 gpui。
- Ubuntu 原生 runner 上 `ec_gpui` / `fig_desktop` 的第一次编译日志（C2；本机无 `aarch64-linux-gnu-gcc`，不能用交叉编译当证据）。
- GNOME Wayland 有没有不依赖 Shell 扩展的 caret（D2；失败则 v1 只支持 X11 / XWayland + IBus）。
- 无桌面 Linux 是否提供「纯 CLI 补全」（建议否，与 macOS `suppress_without_desktop_app` 对齐）。

---

## 7. 每一步的执行协议（plan → 做 → review → fix）

对每一个 PR：

1. **Plan 模式。** 对照本文件该节的「做 / 不做 / 验收」，列出将改的文件。若要改非目标里的东西，先改本计划再动代码。
2. **实现。** 只做该节范围。
3. **Review（对齐目标，不是对齐能否编过考古文件）。** 清单：
   - [ ] 有没有重新引入 WebView / wry / WKWebView？
   - [ ] 无 caret 时有没有用窗口矩形摆放？
   - [ ] 有没有把 AppKit 链进 `fig_util` / `ecterm`？
   - [ ] macOS：`cargo clippy --locked --workspace -- -D warnings` 与相关 `cargo test` 绿？
   - [ ] 新平台：本节列出的 crate 在对应 CI runner 绿？
   - [ ] `ecterm` 的 scrollback / tokio worker 约束还在吗？
   - [ ] 安装/IME 相关改动有没有违反「只 enable、按哈希决定是否换进程」？（macOS 回归）
4. **Fix。** Review 失败不得把里程碑标完成。
5. **更新本文件。** 该节标 `done` + 合入 SHA；开放问题关闭或下移。

macOS 回归是否决项。跨平台进度慢可以接受，把 Otty caret / 外接屏 / `···` 看门狗弄坏不可以。

---

## 8. 当前执行指针

- 分支：`fix/cross-platform-audit-1` @ `c04044ca`（从 `feat/cross-platform` 切出；`feat/cross-platform` 来自 `main` @ `55b043ff`）
- **发运：** 仍只有 macOS Apple Silicon DMG。Linux / Windows 是 WIP，不是产品（无 Linux 包、无 Windows 安装器）。
- 本文件就是 M0 的文档交付物
- 2026-08-24 续 53（`c04044ca`）：审 `999d7954` 发现 `--cli` 对 Fig `cache.cacheKey` listing 漏清（键是 `script:,oxlint-rules` 这类，不是 CLI 名）。`c04044ca` 给 suggestion 缓存键加 `tokens[0]` 前缀；`cache_key()` 本身未改。intercept 热加载 / SharedString / WindowPosition 仍是 `999d7954`。钉：`clear_caches_for_cli_drops_listings_whose_cache_key_is_not_the_cli`。**未改 framing / 无 scan-`\x1b@`。** 补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_engine -p ec_gpui -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 172、ec_engine 258、ec_gpui 106；`ec engine complete --buffer "git ch"` 含 `checkout`；`cargo fmt --all -- --check` 绿。未测 macOS AX/IME/DMG/Sparkle，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。审阅：`/workspace/easy-complete-BUILD-REVIEW-999d7954.md`。
- 2026-08-24 续 52（`999d7954`）：三链深审推迟项。设置/快捷键热加载：浮层已 intercept 时 `ReloadSettings` 补发 `InterceptFigJs`（`refresh_intercept_actions`），不重算 visibility、不发 `InterceptFigJSVisible`；`set_intercept_flags` 仍在 mode 未变时 `continue`，`overlay_actions()` 不回每键。`ClearAutocompleteCache.clis` 传到引擎：空 = 清全部，`--cli git` 只丢 hook-id / first-token 匹配的 generateSpec 与 generator 键。`list.rs` 可见行 `font_family` 改 `SharedString`，paint 借 `OverlayState` 不再每行 clone `String`。删死 `WindowPosition::Centered` / `Absolute`（含 (120,120) 臂）；放置只剩 `RelativeToCaret`，caret park 未动。钉：`unchanged_intercept_modes_skip_figterm_ipc` 扩 reload、`cache_key_belongs_to_cli_matches_hook_id_and_first_token`、`clear_caches_for_one_cli_keeps_the_other`、`merge_cache_clear_filters_empty_means_all`、`suggestion_row_does_not_clone_the_item_on_paint` 扩 font、`apply_position_and_layout_overlay_share_one_screen_list` 扩无 Centered/Absolute。**未改 framing / 无 scan-`\x1b@`。** 补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_engine -p ec_gpui -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 172、ec_engine 257、ec_gpui 106；`ec engine complete --buffer "git ch"` 含 `checkout`；`cargo fmt --all -- --check` 绿。未测 macOS AX/IME/DMG/Sparkle，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。全文：`/workspace/easy-complete-THREE-CHAIN-REVIEW.md`。
- 2026-08-24 三链深审（`b1c36593`）：发运 `dist` 去掉 `panic = "abort"`，引擎 attempt 线程 `catch_unwind` 在 DMG 上仍能隔离 generator panic（原注释「没有 catch_unwind」是假的）。`scripts/build-windows.sh` 加 `--locked`。PLAN §2.1 改为 rust-linux/windows 存在、dist 仍只在 macos-15；§2 标明 2.4 是 2026-08-20 快照。钉：`leftover_crates` 扩 dist unwind + Windows `--locked`、`rust_linux_and_windows_ci` 扩 job 名。**未改 framing / 无 scan-`\x1b@`。** 补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_engine -p fig_install -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline -p fig_desktop` 172；`ec engine complete --buffer "git ch"` 含 `checkout`（debug 二进制 8 次 wall min 50 / median ~66 / max 89 ms，新进程，不拿它当 overlay 按键数字）；`cargo fmt --all -- --check` 绿。未测 macOS AX/IME/DMG/Sparkle，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。全文：`/workspace/easy-complete-THREE-CHAIN-REVIEW.md`。
- 2026-08-23 审计：本机 Linux（x86_64）已验证无头 crate + `ec_gpui`/`fig_desktop` `clippy -D warnings`；修了 `fig_util` HRTB、`ec_gpui` X11 API；README 标明跨平台未发运
- 2026-08-23：`rust-toolchain.toml` 去掉额外 `targets`（避免 Linux/Windows CI 拉 Darwin std）；`rust-linux` **不**装 `shellcheck`（与 macOS job 不同），测试在二进制缺失时 skip；F2 `taskkill /T` + `~user`、F3 `TerminateProcess` BOOL、F4 Win32 caret 换算可在 Linux 单测；`setup.sh` 不再装 WebKit / 不再 `rustup default stable`
- 2026-08-23 续：F5 `SetWindowPos` 策略（不抢焦点、NOSIZE、park=HIDE+NOMOVE、place 顶左取整、缺 HWND / 空虚拟屏则不摆）在 `ec_gpui::windows_overlay` 钉死，`windows.rs` 仍 `cfg(windows)`。named-pipe retry/bind 策略在 `fig_ipc::windows_pipe_policy` 钉死；accept/connect 仍是 `cfg(windows)`。F3 续：ConPTY `HRESULT` 成功是 0，与 `TerminateProcess` BOOL 相反。`rust-windows` 与 `rust-linux` **同一 crate 列表**，是 MSVC 下 ConPTY / named pipe / GPUI HWND 的**编译**，不是桌面会话——GetGUIThreadInfo、对真实 HWND 的 `SetWindowPos`、ConPTY I/O 都没有测。两 job 都在 YAML 里，**第一次 GitHub 原生 run 仍待 push**。
- 2026-08-23 续 2（本机 Ubuntu `DISPLAY=:2`）：`scripts/build-linux.sh` **真的打出了** `dist/linux/easy-complete-2.2.2-x86_64.tar.gz`（`--locked`，IR 840/734/1480）。`install-linux.sh --prefix` 装进临时目录后，前缀里的 `ec engine complete --buffer "git ch"` 含 `checkout`（不设 `EC_SPECS_DIR`）；`--uninstall` 只清该前缀，**不会**删掉指向别的 PREFIX 的 autostart。D3 浮层：无 Vulkan ICD 时 GPUI 0.2.2 在创建窗口前 panic（`NoSupportedDeviceFound`）；装 lavapipe 后进程能住，`--no-dashboard` 且**不注入 caret** 时 xwininfo/xdotool 看不到 `Fig Autocomplete`（浮层只在 show 时才建窗）。没有用窗口矩形兜底。tarball / README 标明 Linux 未发运。
- 2026-08-23 续 3（`DISPLAY=:2` 真终端 caret）：IBus 1.5.32 私有总线没有 `org.freedesktop.DBus.Monitoring`，监听改为 BecomeMonitor 失败则 `AddMatch` `eavesdrop='true'`（与 `dbus-monitor` 相同）。at-spi2 2.56 的 `Name` / `CaretOffset` / `Parent` 走属性，方法作回退。D2：X11 分类终端只在 IBus **已经订阅**时让 AT-SPI 让路。手测：`GTK_IM_MODULE=ibus` 的 xfce4-terminal → `bash (ecterm)` → `git ch`，`Fig Autocomplete` **IsViewable**，再打一个字母浮层 X +10（字符宽）。无窗口矩形兜底。GNOME Wayland 上的 AT-SPI 路径未手测。
- 2026-08-23 续 4（M2 + D3）：泛 `CI=true` **不再**挡住 wrap（只拦 `GITHUB_ACTIONS` / `Q_CI`；不要把 `Q_FORCE_FIGTERM_LAUNCH` 写进 rc）。本机 `ecterm` + bash/zsh/fish OSC 697 钩子把 `git ch` 送到 mock `remote.sock`，引擎 stdout 含 `checkout`（无 caret、无浮层）。D3：map 后再发 `_NET_WM_STATE_ABOVE`；`ec-overlay-spike` 在 `:2` 上 `_NET_WM_WINDOW_TYPE_NOTIFICATION` + ABOVE，且不是 `_NET_ACTIVE_WINDOW`。`setup.sh` 仍只装编译依赖（GTK 托盘 / X11 / Vulkan 头），不装 WebKit；caret 运行时依赖 IBus/AT-SPI 不写进 apt。
- 2026-08-23 续 5（`0ddd0de2` 审计收口）：`platform/mod.rs` 活路是 macos / linux_caret / windows_caret / stub。考古 `platform/linux/**` 与 `platform/windows.rs` **已删除，勿恢复、勿翻 cfg**。`local_webview_data_dir` 已去掉；Linux uninstall 只清 `easy-complete` 数据前缀。figterm history SQLite 走 `spawn_blocking`。desktop IPC `send_event` 关闭环时 warn 而不是 unwrap。`get_current_buffer` 原地 truncate。同一次 complete 快照 `AutocompleteFlags`。非 Mac 屏幕列表改名 `overlay_screens`。`GLOBAL_PROXY.set` / `spawn_engine` 失败走可诊断退出。本机未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 6（`54dee500`）：本机 Ubuntu 上 `cargo test -p fig_desktop` 全绿（115 + 1 ignored）。AppImage autostart 走 `set_enabled_in(ctx)`：AppImage 链到本地 desktop entry（不是 FUSE `current_exe`），前缀安装写 `--is-startup` 文件；`app.launchOnStartup` 仍是门闩。设置权限门在非 macOS 只看 shell integration，不因 Accessibility/IME 卡住。figterm mutex/rwlock poison 恢复；`UnixTerminal::Drop` warn 而不是 unwrap；history SQLite 改独立 `std::thread`（不再用 forever `spawn_blocking` 占 blocking pool）。去掉空的 `WryIdMap` / JS handler channel。`webview/` 模块名未改（rename churn）。本机未测 macOS AX/IME/DMG，未假装 Windows live runtime。Linux tarball 已按变更二进制重打。
- 2026-08-23 续 7（`8ba16257`）：P1-2 已关。`fig_desktop/src/webview/` 改名为 `bootstrap/`，`WebviewManager` → `AppRuntime`，通知状态去掉 WebView 前缀。行为不变。`fig_desktop_api` 仍由 `fig_desktop` 链接（macOS 发运），与 `ec_overlay_spike` 都**不**在 `default-members`；dist 脚本不打 spike。本机 `cargo test -p fig_desktop` 116 + 1 ignored。本机未测 macOS AX/IME/DMG，未假装 Windows live runtime。Linux tarball 已按变更二进制重打。
- 2026-08-23 续 8（review `8ba16257`）：干净，无代码修复。无残留 `crate::webview` / `WebviewManager` / `WebviewNotificationsState`；`src/webview/` 不存在。macOS 菜单 / ActivationPolicy / Sparkle / GPUI host 只改了类型名。留下的 “webview” 字样不是 bootstrap 模块：Sparkle `show_webview`（弹更新 UI）、`ec debug devtools` 帮助文案、以及对照旧 WebView 行为的单测名。本机 `cargo test -p fig_desktop` 116 + 1 ignored；`fig_desktop_api` 3 passed；`clippy -D warnings` on fig_desktop / fig_desktop_api / fig_util / ec_overlay_spike 绿；`ec engine complete --buffer "git ch"` 含 `checkout`；`cargo fmt --all -- --check` 绿。未测 macOS AX/IME/DMG，未假装 Windows live runtime。未开 remote_ipc unwrap / tokenize / X11 cache。
- 2026-08-23 续 9（`719377b3`）：R2-4 / R2-5 / R2-3。`remote_ipc` 关闭环走 `send_event_or_warn`，protobuf encode 失败 `error!` + continue，不再 naked unwrap（`rg 'send_event\(.*unwrap' crates/fig_desktop` 只剩测试）。`figterm` `EventHandler` 的 `main_loop_sender` 与 socket/history 一样 `error!`。`Engine::complete` 只 `tokenize` 一次，把 `(tokens, ends_with_space, buffer)` 传给 `lookup::complete`，ranking root 复用已解析 token。未开 X11 cache / history OnceLock / README / overlay title。本机 `cargo clippy --offline -p fig_desktop -p figterm -p ec_engine -- -D warnings` 绿；`cargo test --offline -p fig_desktop` 117 + 1 ignored；`figterm` 37 + 1 ignored + cli 1 + linux_shell_hooks 3；`ec_engine` 212；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 10（review `719377b3`）：干净，无代码修复。`edit_buffer` 订阅循环 encode 失败 `error!`+continue，关闭环 `send_event_or_warn`；同函数 `GpuiOverlayBuffer` / `PlatformBoundEvent` 以及 `broadcast_notification_all` 仍是 `send_event()?` / `encode?`（Result，不是 panic）。`EventHandler` Prompt/PreExec 关闭 `main_loop_sender` 走 `error!`，`closed_main_loop_sender_does_not_panic` 覆盖；`figterm/src/ipc.rs` SSH flush 仍有 `main_loop_sender.send(...).unwrap()`，未开。`Engine::complete` 对 `completion_buffer` 只 `tokenize` 一次再传入 lookup；`tokens.first()` 与 overlay 记 acceptance 用的 `ranking_root_command` 同式。源码钉之外有 `chained_buffer_ranks_and_merges_history` 行为钉。本机 `cargo clippy --offline -p fig_desktop -p figterm -p ec_engine -- -D warnings` 绿；`cargo test --offline -p fig_desktop` 117 + 1 ignored；`figterm` 37 + 1 ignored + cli 1 + linux_shell_hooks 3；`ec_engine` 212；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。未开 X11 cache / history OnceLock / README / overlay title。
- 2026-08-23 续 11（`d010ebb5`）：R2-1 / R2-2 / SSH flush unwrap。`ec_gpui/src/linux.rs` 缓存 `(conn, screen_num)`，失败重试与 `overlay_screens` 共用 500ms TTL；place/probe 只留一处 `RustConnection::connect`。`apply_position` 取一次屏幕列表传给 `layout_overlay` / `overlay_bounds`，不用窗口矩形。figterm history `OnceLock` 单队列单线程，调用方 clone Sender，`history_thread_builder_runs_once` 钉 Builder 只跑一次；`TERM_SCROLLBACK_LINES` 仍为 1。`ipc.rs` SSH flush 关闭 `main_loop_sender` 走 `error!`，与 EventHandler 相同。未开 README 分支名 / 浮层标题。本机 `cargo clippy --offline -p ec_gpui -p fig_desktop -p figterm -- -D warnings` 绿；`cargo test --offline`：ec_gpui 72；fig_desktop 118 + 1 ignored；figterm 40 + 1 ignored + cli 1 + linux_shell_hooks 3；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 12（review `d010ebb5` → `b332d9ca`）：X11 缓存缺 flush / 死连接不丢 / 空 screens TTL 盖住已连上的 display；history OnceLock 在 spawn 失败时会存断线 Sender。`with_x11` 回调后 `flush`，失败 `discard_display`；成功 connect 清 screens；空列表且已有 display 则重查。history spawn `expect`（OnceLock panic 可重试），测试钉 `receiver_count > 0`。SSH flush / `TERM_SCROLLBACK_LINES == 1` / 无窗口矩形 caret / 单处 `RustConnection::connect` 保持。未开 README 分支名 / 浮层标题。本机 `cargo clippy --offline -p ec_gpui -p fig_desktop -p figterm -- -D warnings` 绿；`cargo test --offline`：ec_gpui 72；fig_desktop 118 + 1 ignored；figterm 40 + 1 ignored + cli 1 + linux_shell_hooks 3；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 13（`36a322ea`）：N1 / N2 / N5 / N4。README 与 README.zh-CN 的 WIP 分支改为 `fix/cross-platform-audit-1`；仍写明无 Linux 包、无 Windows 安装器、CI 只是门禁。浮层标题 `Fig Autocomplete` → `Easy Complete`（`ec_gpui::OVERLAY_WINDOW_TITLE`，Linux 按标题 find 用同一常量；`AUTOCOMPLETE_WINDOW_TITLE` 别名，钉死不含 Fig）。`show_webview` → `show_updater`（Sparkle 提示，不是 WKWebView）。非 Mac `quartz_y_to_cocoa_frame_y` → `screen_y_to_frame_y`；`caret_y_in_quartz_space` → `caret_y_in_screen_space`；macOS 仍保留 Cocoa `quartz_y_to_cocoa_frame_y`。补全行为 / caret 策略 / scrollback=1 / 考古后端未动。本机 `cargo clippy --offline -p ec_gpui -p fig_desktop -- -D warnings` 绿；`cargo test --offline`：ec_gpui 75；fig_desktop 118 + 1 ignored；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 14（review `36a322ea`）：干净，无代码修复。Linux place/park/harden 全部走 `OVERLAY_WINDOW_TITLE`（`"Easy Complete"`，不含 Fig）；设置窗仍是 `"Settings"`，find-by-title 不会撞。Windows 不按标题找（`windows_titled_overlay_places() == false`），HWND 标题由同一常量 `set_window_title`。Y 公式未改：`screen_y_to_frame_y` / macOS `quartz_y_to_cocoa_frame_y` 仍是 `origin+height-y-h`（100/140/0/900 → 660）；Linux ConfigureWindow 与 Win32 `SetWindowPos` 仍用顶左，不套 Cocoa 翻转；`caret_y_in_screen_space` BottomLeft 120/18/900 → 762。`show_updater` 无 `show_webview`；`src/webview/` 不存在；剩下的 webview 字样是钉「旧名已走」或对照旧行为的单测。`Failed to open` / `has started` 用 `PRODUCT_NAME`。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端未动。N3（`matches_webview` 测名）未开。本机 `cargo clippy --offline -p ec_gpui -p fig_desktop -- -D warnings` 绿；`cargo test --offline`：ec_gpui 75；fig_desktop 118 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。
- 2026-08-23 续 15（`d114d993`）：N3 + tokio + 用户可见 Fig/WebView 扫尾。`ec_engine` 测名 `matches_webview` / `like_the_webview` 改为 native 描述（`clean_output_strips_cr_ansi_and_blank_lines`、`script_timeout_default_and_max_priority`、`split_on_wins_over_post_process`、`omitted_priority_defaults_to_fifty`、`string_query_term_uses_separator_suffix`；`execute_command_maps_command_args_cwd_env_timeout` 一并改）；行为未动，`js_host_generate_runtime_tests_do_not_use_webview_fn_names` 钉旧名不再出现。工作区 `tokio` 从 `full` 收成 `fs, io-std, io-util, macros, net, process, rt, rt-multi-thread, signal, sync, time`（去掉 parking_lot 后端；`net` 覆盖 Unix socket 与 Windows named pipe，`signal` 覆盖 SIGWINCH / ctrl_c）。CLI：`ec debug devtools` 不再说 webview，`debug build` 不再说 Fig.js；`fig update` / `fig settings` / `fig launch` / `fig issue` 提示改走 `CLI_BINARY_NAME`；ecterm logger / IME 非 macOS stub / uninstall warn 用 `PRODUCT_NAME`。网站 How-it-works：GPUI 浮层+设置，crate `ec_cli`（不再 wry WebView / `q_cli`）。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p figterm -p fig_desktop -p fig_ipc -p fig_install -p ec_cli -p fig_input_method -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 213；fig_desktop 118 + 1 ignored；figterm 40 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_ipc 16；fig_install 15；ec_cli 28 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。Darwin 交叉编 `fig_ipc`/`ec_engine` 卡在本机 cc 不认 `-arch`（sqlite），不是源码红。无 `x86_64-pc-windows-msvc` target，未假装 Windows live。网站 `node_modules` 不在，未跑 `pnpm build`。
- 2026-08-23 续 16（review `d114d993`）：干净，无代码修复。N3：`matches_webview` / `like_the_webview` 不再是活测名（`cargo test -p ec_engine -- --list` 只剩钉 `js_host_generate_runtime_tests_do_not_use_webview_fn_names` 与 ranking 钉 `match_buckets_follow_webview_exact_prefix_order`）；钉用 `join("_")` 所以 `include_str` 自身不含旧名；六个测体未改。Tokio：1.50 `default = []`，工作区列表即 `full` 去掉 `parking_lot`；`net` 覆盖 `UnixListener`/`UnixStream`、Windows `named_pipe`（tokio 还拉 `Win32_System_Pipes`）、`AsyncFd`；`signal` 覆盖 SIGWINCH / `ctrl_c`（Windows `Win32_System_Console`）；`io-std` 覆盖 stdin/stdout；figterm 仍 `new_multi_thread().worker_threads(2).enable_all()`。无 `time::pause`，不补 `test-util`，不恢复 `full`。CLI 提示走 `CLI_BINARY_NAME`（`ec`）；ecterm / uninstall 走 `PRODUCT_NAME`；IME 非 macOS stub 硬编码 `Easy Complete`（该 crate 不链 `fig_util`）。网站 How-it-works 无 wry/WebView/`q_cli`；`/fig-alternative` 遗产营销保留。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端未动。预存在：overlay 对 `WindowEvent::Devtools` 仍 ignore（帮助已不说 webview），本轮不接。本机 Linux `cargo clippy --locked --offline -p ec_engine -p figterm -p fig_desktop -p fig_ipc -p fig_install -p ec_cli -p fig_input_method -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 213；fig_desktop 118 + 1 ignored；figterm 40 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_ipc 16；fig_install 15；ec_cli 28 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。未开 Suggestion-clone / spike isolation。
- 2026-08-23 续 17（`4f07d1f4`）：ranking 测名 `match_buckets_follow_webview_exact_prefix_order` → `match_score_buckets_exact_then_prefix`（exact/prefix 桶体未改）；`ranking_tests_do_not_use_webview_fn_names` 用 `join("_")` 钉旧名；`js_host_generate_runtime_tests_do_not_use_webview_fn_names` 扩到 `query.rs` / `rank.rs`。`ACTIVATION_POLICY`（gpui_host / bootstrap / macos）与 `DEBUG_MODE`（local_ipc/commands）poison 走 `recover_mutex` → `into_inner` + warn，不再 `lock().unwrap()`；`mutex_lock_recovers_from_poison` + 源码钉。`utils.rs` 捆绑 Icon `expect` 改为 warn/None；`bundled_icon_png_decodes` 钉 happy-path 仍解出 `icon.png`。macOS 托盘仍走 `tray.rs` `get_icon`，本函数不在发运路径。Perf：本机 release `ec engine complete` 8 次 wall（含进程启动 + IR + git `jsGenerateSpec`）：`git ch` min 28.5 / median 32.4 / max 49.7 ms；`git ` 27.4 / 28.8 / 30.8；`git checkout -- ` 28.9 / 39.4 / 72.6；`ls -` 16.3 / 17.6 / 23.1。浮层 mapping 已 `into_iter` 搬 Suggestion；file icon 仅 macOS 且有缓存。`match_score` 的 `to_lowercase` 与 `AutocompleteFlags::snapshot` 每次 `OldSettings::load`（CLI 不 `init_global`；桌面有）未改——相对 generateSpec / 进程启动看不出，未动 `Suggestion::new`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 214；fig_desktop 121 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。macos `focused_window.lock().unwrap()` 与 `tray.rs` `get_icon` expect 未开。
- 2026-08-23 续 18（review `4f07d1f4`）：干净，无代码修复。活测名是 `match_score_buckets_exact_then_prefix`（桶体 ExactCase / ExactInsensitive / PrefixCase / PrefixInsensitive 未改）；`cargo test -p ec_engine -- --list` 无 `match_buckets_follow_webview_exact_prefix_order` / `matches_webview` / `like_the_webview`，只剩钉 `ranking_tests_do_not_use_webview_fn_names` 与 `js_host_generate_runtime_tests_do_not_use_webview_fn_names`（`include_str` 覆盖 query.rs / rank.rs，`join("_")` 所以钉自身不含旧名）。`recover_mutex` 只接 `ACTIVATION_POLICY` 与函数内 `DEBUG_MODE`（都是 `std::sync::Mutex`，enum/bool 赋值，poison 无负载语义）；`focused_window.lock().unwrap()` 仍在 macos.rs，本轮不接。DEBUG_MODE：set / toggle / get 互斥臂，锁不跨 await；IPC 客户端 `set` 与 `toggle` 不同时发。`utils::icon` → `Option` 只有测试调用；Linux `linux_tray` / 非 Linux `gpui_host` 仍 `tray.rs` `get_icon`。`bundled_icon_png_decodes` 过；`test_icon` 在 Linux 仍 ignore。`Suggestion::new` / `rank.rs` / caret / `TERM_SCROLLBACK_LINES == 1` 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 214；fig_desktop 121 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。未开 focused_window / tray `get_icon`。
- 2026-08-23 续 19（`94d6b2b2`）：macos `focused_window` 六个 `lock().unwrap()` 改走 `recover_mutex`（`into_inner` + warn，行为仍是同一把 `Option<PlatformWindowImpl>`）；`activation_policy_and_debug_mode_recover_from_poison` 用 `include_str!("platform/macos.rs")` 钉 `focused_window.lock().unwrap()` 不再出现、且含 `recover_mutex(&self.focused_window)`（不需要 AppKit）。未改：`UNMANAGED.event_sender` / `window_server` 的 `RwLock` unwrap（同类 poison，但要新的 `recover_rwlock_*`，Linux 上无调用会 `-D dead_code`）；`GLOBAL_PROXY.get().unwrap()` 是 OnceLock 未初始化，不是 poison；`CString::new` 是编码串带 NUL。`tray.rs` `get_icon` → `Option<Icon>`：`decode_tray_icon` 坏 PNG / `from_rgba` 失败 warn+None，OnceLock 缓存 Option；`build_tray_icon` 无 icon 仍建托盘；Linux `linux_tray` 与非 Linux `gpui_host` `set_icon` 直接吃 Option（`TrayIcon::set_icon` 本就 `Option`）。`warning_icon_rgba` 同一 expect 一并降级（菜单缺黄点，不 panic）。Happy-path PNG 仍解出；`invalid_tray_icon_bytes_do_not_panic`；源码钉用 `concat` 拼旧 expect 字面量。**fig_ipc A3 研究，不改 framing。** 帧是 `\x1b@` + 8 字节类型 + BE u64 长度 + body。今日 desync：非 `Incomplete` 的 parse 错推进 `cursor.position()`（`InvalidHeader` / `InvalidMessageType` 通常 10 字节）后 `Err`。desktop `local_ipc`（`unwrap_or_else` → `None` 退出循环）、figterm `ipc`（`Err` → `break`）、remote handshake / incoming 都把 `RecvError` 当断线，残留 buffer 随 socket 丢掉，所以不会在同一条流上无限错位。最小 resync（扫下一个 `\x1b@` 并 continue）会把「丢连接」改成「继续读」；protobuf 体里可以出现 `\x1b@`，假边界后的长度字段还能逼出巨大 `Incomplete(Body)` 读。协议兼容性不清晰，不投机改帧。钉：`invalid_header_advances_ten_bytes_and_errors`（恰好 10 字节垃圾后 *retry* 能对齐，生产不 retry）；`one_byte_desync_does_not_resync_to_next_frame`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p fig_ipc -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 123 + 1 ignored；fig_ipc 18；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘，未假装 Windows live runtime。
- 2026-08-23 续 20（review `94d6b2b2`）：干净，无代码修复。macos `focused_window` 六个 site 都是 `recover_mutex(&self.focused_window)`（ExternalWindowFocusChanged / AutocompleteWindowLevelUpdateRequested / FocusedElementChanged / WindowDestroyed / `refresh_window_position` / `get_active_window`），无残留 `focused_window.lock()`；`Option<PlatformWindowImpl>` 仍 replace/take/as_ref/as_mut/clone。`activation_policy_and_debug_mode_recover_from_poison` 钉 `focused_window.lock().unwrap()` 与 `recover_mutex(&self.focused_window)`。留下的 macos unwrap 与续 19 一致：`UNMANAGED.event_sender` / `window_server` RwLock（需 `recover_rwlock`，Linux `-D dead_code`）、`GLOBAL_PROXY.get().unwrap()`（OnceLock）、`CString::new`（NUL）。Option icon 不炸托盘：`TrayIconBuilder` 无 icon 仍 `build()`；Linux `linux_tray` 与非 Linux `gpui_host` `set_icon(get_icon(...))` 吃 `Option`（tray-icon `set_icon` 本就 Option；macOS `setImage(None)`，Linux 不写 PNG 仍 `set_icon_full`，Windows 空 HICON；无图时 `set_icon_as_template` 跳过）；`MenuElement::info` 吃 Option，黄点缺不 panic。`invalid_tray_icon_bytes_do_not_panic` / `tray_icon_decode_is_cached` / happy-path PNG Some。fig_ipc framing 未改：只删 TODO、加注释、加两测；`InvalidHeader` 仍 advance `cursor.position()`（10 字节）后 Err。desktop `local_ipc` unwrap_or_else None 退出；figterm `Err` break；remote handshake/incoming None/break；`fig_remote_ipc` Err break。无 scan-`\x1b@`。PLAN 续 19 与代码一致。`rank.rs` / caret / `TERM_SCROLLBACK_LINES == 1` 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p fig_ipc -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 123 + 1 ignored；fig_ipc 18；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘，未假装 Windows live runtime。未开 `event_sender` / `window_server` RwLock poison。
- 2026-08-23 续 21（`3cb1427d`）：macos `UNMANAGED.event_sender` / `window_server` 三个 lock unwrap 改走 `recover_rwlock_read` / `recover_rwlock_write`（`into_inner` + warn，与 `recover_mutex` 同精神）。helpers 在 `utils.rs` 且 `cfg(any(macos, test))`，Linux `-D dead_code` 看不到。**未改事件循环。** `GLOBAL_PROXY.get().unwrap()`（OnceLock 未初始化）、`CString::new`（NUL）、event_sender 的 Option `.clone().unwrap()` 仍在。`unmanaged_rwlocks_recover_from_poison` 用 `include_str!("platform/macos.rs")` 钉 `.write().unwrap()` / `.read().unwrap()` 不再出现，且含 `recover_rwlock_write(&UNMANAGED.event_sender)` / `window_server` 与 `recover_rwlock_read(&UNMANAGED.event_sender)`（不需要 AppKit）。`rwlock_lock_recovers_from_poison` 真 poison。`file_watcher` `recommended_watcher` 失败 `warn!` + `return`，不再 `.unwrap()` 炸桌面；`settings_watcher_creation_does_not_panic` concat 钉旧 `)\n    .unwrap();`。`ec_gpui` icons：`cached_image("box").expect` / `cached_image("history").expect` 改为 empty slot + warn，happy-path 仍走 `png_icon_element`。`named_bundled_icons_still_decode` 钉 box/folder/file/symlink/git/command PNG 魔数+IHDR、history SVG、`cached_image` Some；concat 钉旧 expect 字面量。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_gpui -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 126 + 1 ignored；ec_gpui 76；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live runtime。未 push。
- 2026-08-23 续 22（review `3cb1427d`）：干净，无代码修复。macos `UNMANAGED.event_sender` / `window_server` 三个 site 都是 `recover_rwlock_*`（Initialize 两次 write、caret 一次 `event_sender` read），无残留 `.write().unwrap()` / `.read().unwrap()`。helpers `#[cfg(any(target_os = "macos", test))]`，Linux `clippy -D warnings` 绿（无 unused）。`rwlock_lock_recovers_from_poison` 真 poison；`unmanaged_rwlocks_recover_from_poison` include_str 钉 macos.rs。留下的 unwrap 与续 21 一致：`GLOBAL_PROXY.get().unwrap()`（OnceLock）、`CString::new`（NUL）、event_sender Option `.clone().unwrap()`。`file_watcher` `recommended_watcher` 失败 `warn!`+`return`（live-reload 关，桌面不炸）；生产路径无 `.unwrap()`。`named_icon_element` / `history_icon_image_element` 缺图 empty slot + warn，返回 `AnyElement`；happy-path 仍 `png_icon_element`；`named_bundled_icons_still_decode` 钉 PNG 魔数+IHDR、history SVG、`cached_image` Some，concat 钉旧 expect。list 仍 `.child(history_icon_image_element)`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_gpui -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 126 + 1 ignored；ec_gpui 76；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live runtime。未开 figterm intercept unwrap / `max_blocking_threads` / `AutocompleteFlags` 单次 load / Linux overlay intern。
- 2026-08-23 续 23（省约 R3 `0d8f0f19` / `bb8edf9c` / `159659a7`）：P0 figterm intercept `remote_sender.send(hook_to_message(hook)).unwrap()` → `if let Err` + `error!("Sender error")`，与 EventHandler 相同；`intercept_remote_sender_does_not_unwrap` concat 钉旧 unwrap，`rg` 该字面量为空。P1 ecterm runtime `.max_blocking_threads(8)`（与 desktop 同），`worker_threads(2)` 未改；`tokio_runtime_caps_blocking_threads` 钉两处。P1 `AutocompleteFlags::snapshot` 一次 `OldSettings::load` + `map().clone()`，`from_map` 本地读，不再 `Settings::new` / 每旗 `get_bool_or`（CLI 不 `init_global` 时以前每次 complete 读文件 ~10 次）；`snapshot_loads_settings_map_once` 源码钉 + 空 map 等于 `Default` + 有值 map 读 hide/sort/custom/disableForCommands。P1 Linux overlay：`harden` 走 `overlay_x_window`；非 0 的 `CACHED_X_WINDOW` 信任到 map/configure/unmap 失败或 `invalidate_cached_overlay_x_window`（place 不再每次 `WM_NAME`）；`intern` 用 `OnceLock<Mutex<InternedAtoms>>`；`DPI_SCALE` OnceLock 在第一次成功 Xft 读取后命中；`overlay_trusts_cached_x_window_and_interns_once` 钉。`find_window_by_title` 仍是 cache miss 路径；单处 `RustConnection::connect` / flush / 无窗口矩形 caret / `TERM_SCROLLBACK_LINES == 1` 保持。补全 / caret 策略 / 考古后端 / tokio feature 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p figterm -p ec_engine -p ec_gpui -- -D warnings` 绿；`cargo test --locked --offline`：figterm 42 + 1 ignored + cli 1 + linux_shell_hooks 3；ec_engine 215；ec_gpui 77；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live runtime。未 push。
- 2026-08-23 续 24（`05758055` / `57ba84b0` / `5634ce82` / `a86affe1`）：正确性 + 内存 + 用户可见命名。figterm 本地 IPC `incoming_tx.send_async.unwrap()` → `error!("Sender error")` + break（不再每条 clone Sender）；`local_ipc_incoming_send_does_not_unwrap` 钉 `spawn_figterm_ipc` 体无 `.unwrap()`。fig_log 三把 `Mutex` poison 走 `recover_mutex` `into_inner`（ecterm / desktop / CLI 共用）；`log_level_locks_recover_from_poison` 真 poison + 源码钉。JS `executeCommand`（`execute_full`）Unix 不再 `wait_with_output` 辅助线程：poll stdout+stderr、读时截断 256 KiB，满则杀进程组，避免钩子把引擎 worker OOM；Windows 仍 threaded waiter。钉：stdout/stderr、env、timeout、cap、unix 路径源码无 `thread::spawn`。macos `SetEnabled` send_event unwrap → warn（与 reopen 相同）；`event_sender` Option `.clone().unwrap()` → `if let Some` + warn；`macos_overlay_sends_do_not_unwrap` include_str 钉（不需要 AppKit）。留下 `GLOBAL_PROXY.get().unwrap()`（OnceLock）、`CString::new`（NUL）。用户可见 qterm：disable note / doctor 走 `{PTY_BINARY_NAME}`（ecterm）；设置键 `qterm.enabled`、env `QTERM_SESSION_ID`、analytics `qterm_socket_check` 未改。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p figterm -p ec_engine -p fig_log -p fig_desktop -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：figterm 44 + 1 ignored + cli 1 + linux_shell_hooks 3；ec_engine 220；fig_log 2；fig_desktop 127 + 1 ignored；ec_cli 29 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live runtime。未 push。
- 2026-08-23 续 25（`81d4ea7b`）：热路径 + CI 诚实。`match_score` ASCII 走 `eq_ignore_ascii_case` / 前缀字节，跳过两次 `to_lowercase`；非 ASCII 仍 Unicode fold（`İ` PrefixInsensitive 钉）。`rank::apply_with_acceptance` 每行算一次 `MatchScore` 再 `sort_by`，比较时不再重跑 scorer。history / suffix `HashSet<&str>`；`merge_history` 同样。filegen `compare_file_names` 用 `flat_map(char::to_lowercase)`，不再每字 `String`；`entry_is_dir` 走 `DirEntry::file_type`，symlink 仍 `path().is_dir()`（`follows_symlink_directories`）。钉：`compare_file_names_matches_concatenated_to_lowercase`、`ascii_match_score_keeps_exact_then_prefix_buckets`。IME vs AX `hide_overlay_on_element_change` 从 macos.rs 搬到 `platform/caret.rs`（无 AppKit）；Linux 钉 Otty/Ghostty/Kitty 不藏、iTerm/Terminal/VSCode 藏；`include_str` 钉 macos.rs 不再定义该 fn。CI：`rust-linux` / `rust-windows` clippy+test 加 `fig_log`；`cargo test -p fig_desktop` 整套（含 linux_caret；无 dbus-daemon 则 mock-bus skip），不再 `-- platform::caret` / `-- overlay::`。`rust_linux_and_windows_ci_run_desktop_host_tests` 钉。本机有 dbus-daemon，linux_caret mock-bus **真跑**，仍不是 live IBus/AT-SPI 会话。debug CLI `git ch` 仍被进程启动 + generateSpec 主导（min 43.1 / median 68.1 ms），不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 222；fig_desktop 130 + 1 ignored；fig_log 2；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live runtime。未 push。
- 2026-08-23 续 26（`36fd661e`）：cfg 诚实 + Linux 可钉的 Win/macOS 政策。`macos.rs` 仍 `cfg(macos)` 打 AppKit；Quartz→Cocoa、`NSScreen.screens[0]`（不是 `mainScreen`）、非激活 panel / join-all-spaces、frame-echo 日程（IgnoreEcho vs Enqueue，防 livelock）搬进 `macos_overlay`（与 `linux_overlay` / `windows_overlay` 同形，每 OS 编译）。`include_str` 钉 macos.rs 走 `macos_primary_screen_index()`、无 `msg_send![class!(NSScreen), mainScreen]`、无第二份 `fn quartz_y_to_cocoa_frame_y` / `fn decide_overlay_frame_schedule`。Win32：`win32_caret_from_gui_thread` 缺 `hwndCaret` 返回 None（不是窗口矩形）；`win32_caret_poll_action` 丢 caret 才 Hide；`windows_caret.rs` 走这两处。named-pipe `windows_pipe.rs` include_str 钉 bind `first_pipe_instance` / accept 预建下一实例 / retry FILE_NOT_FOUND+PIPE_BUSY。ConPTY `CreatePseudoConsole` / `ResizePseudoConsole` include_str 钉 `win32_hresult_succeeded`，`TerminateProcess` 仍 BOOL。Windows `execute_full` **仍** `wait_with_output` 辅助线程（截断在 wait 之后）；`windows_execute_full_still_waits_on_a_helper_thread` 钉这个缺口，不宣称 poll。`rust_linux_and_windows_ci_run_desktop_host_tests` 扩到 `cargo test -p ec_gpui` 两边都跑。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_gpui -p fig_desktop -p figterm -p ec_engine -p fig_ipc -- -D warnings` 绿；`cargo test --locked --offline`：ec_gpui 98；fig_desktop 133 + 1 ignored；figterm 45 + 1 ignored + cli 1 + linux_shell_hooks 3；ec_engine 223；fig_ipc 19；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 27（`7cedbe6f`）：complete/overlay/IPC 热路径。`Spec.subcommands` 与 `ArgSpec.resolved_spec` 改 `Arc`：`git ch` 列子命令与 `git checkout` walk 只 bump 指针，不再深拷贝 ~111 节点的 IR 树；listing 用 `sorted_by_names` 排引用；`loadSpec` walk 复用 resolved Arc。`nested_subcommands_are_shared_arcs` / `listing_and_walk_do_not_deep_clone_subcommand_trees` 钉。ranking 每行一次 `best_match` + `effective_priority_score` 再 `sort_by`（续 25 只预计算了 MatchScore）。`AutocompleteFlags::snapshot` 透过 settings guard 读，不再 `map().clone()` 整份 JSON。IPC：`encode_fig_protobuf` 一次写入 framed buffer（与 `encode_to_vec`+header 字节相等，钉 `test_to_fig_pbuf`）；`FigMessage::encode` 不再 `body.to_vec()`；recv 走 `take_from_bytes_mut` 从 BytesMut `split_to` 体，Incomplete 不消耗，InvalidHeader 仍前进 10 字节。**未改 framing / 无 scan-`\\x1b@`。** `git#generateSpec` 仍无 cacheKey，每键仍跑 JS——debug CLI `git ch` 仍被 generateSpec + 进程启动主导，不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_proto -p fig_ipc -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 226；fig_proto 15；fig_ipc 20；fig_desktop 133 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 28（`6cffd02d`）：架构边界 + 命名债。`fig_desktop_api` 整 crate 删掉：install/status 搬进 `fig_desktop::install_request`（权限门 / local IPC 仍走同一 dispatcher）。从未写入的 WebView `NotificationsState` 订阅表、`FigIdMap`、`WindowEvent::Emit` / `EmitEventName` / `WindowEvent::Event` 去掉；platform `handle` 不再传空 window map / 空 notify 状态；remote_ipc 不再 protobuf+base64 广播给不存在的窗口（edit buffer 仍发 `GpuiOverlayBuffer`，pre_exec 仍 Hide）。内部窗口 id `DASHBOARD_ID` → `SETTINGS_ID`（`"settings"`）；`dashboard.language` 设置键、`DumpStateWebNotifications` proto/CLI、`DevtoolsDashboard` proto **未改**。AppImage 路径走 `DESKTOP_ENTRY_NAME` / `APP_PROCESS_NAME`，不再硬编码 `q-desktop.desktop` / `q-desktop.png`。删除 `tauri.conf.json`、空 `custom-protocol` feature、`html/offline.html`。钉：`leftover_crates_are_not_default_workspace_members`（crate 目录不存在）、`settings_window_id_is_not_the_webview_dashboard`、`appimage_bundle_paths_use_product_desktop_entry_names`（`join` 所以钉自身不含旧文件名）。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。`qterm.enabled` / `QTERM_SESSION_ID` 未改。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p fig_util -p fig_integrations -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 136 + 1 ignored；fig_util 33 + 1 ignored；fig_integrations 23；ec_cli 29 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 29（`22ff60ea`）：生产路径 panic 降级。macos `GLOBAL_PROXY.get()`（OnceLock，dock reopen C 回调）与 ObjC `CString` 编码走 Option：缺 proxy / 内嵌 NUL 则 warn+skip，不炸桌面。`c_string_without_nul` 在 `utils.rs` `cfg(any(macos, test))`，Linux 可钉。bootstrap：platform Initialize / GPUI host 失败 `diagnostic_exit`（与 `GLOBAL_PROXY.set` / `spawn_engine` 同形）；`local_remote_socket_path` 失败 skip remote IPC；`runtime.run` 失败 `ExitCode::FAILURE`。ecterm：空 wrap argv / argv NUL / `execvp` 走 `Err`；ConPTY `try_clone` 失败 `io::Error`，`clone_killer` 降级 `FailedChildKiller`（poll 路径本来就 `?`）；keymap 空节点 `None` 而不是 panic；UTF-8 空切片 `decode_one_char` 为 None。lookup option-arg walk `take()`。legacy history 缺 data dir skip 文件。gzip IPC encode `?`。**未改 framing / 无 scan-`\\x1b@`。** `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。钉：`macos_overlay_sends_do_not_unwrap`、`c_string_without_nul_rejects_interior_nul`、`bootstrap_startup_does_not_unwrap`、`desktop_run_does_not_unwrap_the_event_loop`、`conpty_create_and_resize_use_hresult_not_bool`（扩 try_clone）、`compressed_encode_does_not_unwrap`、`empty_command_does_not_panic`、`wrap_argv_with_interior_nul_does_not_panic`、`lookup_empty_key_on_empty_map_does_not_panic`、`decode_one_char_empty_does_not_panic`、`legacy_history_write_does_not_unwrap_data_dir`、`option_arg_walk_does_not_expect`。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p figterm -p fig_ipc -p ec_engine -p fig_settings -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 139 + 1 ignored；figterm 50 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_ipc 21；ec_engine 227；fig_settings 15；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 30（`4a2fcea8`）：generateSpec 进程内缓存 + Windows 钩子输出封顶 + 托盘/菜单 append 降级。无 `generateSpecCacheKey` 时不再每键跑 JS：钩子源码第一参数在函数体里不出现（git `help -a`、hub）则 key 是 hook+cwd，`git ch` / `git che` 共用一次结果；mask `--maskfile`、pnpm extra args 仍把 token 列表放进 key。显式 `generateSpecCacheKey` 仍是 `{first}:{key}`。`apply_generate_spec` 一律走 `cached_spec`。钉：`hook_source_ignores_first_param_on_git_not_mask_or_pnpm_install`（含 bundled git/hub/mask/pnpm_2）、`generate_spec_cache_key_drops_tokens_when_the_hook_ignores_them`、`generate_spec_without_cache_key_runs_once_when_tokens_are_unused`（unix，`demo a`/`demo b` 只跑一次 hook）、`generate_spec_without_cache_key_reruns_when_tokens_are_used`、`generate_spec_always_goes_through_the_spec_cache`。Windows `execute_full` / `execute` **仍**辅助线程（不宣称 PeekNamedPipe poll）；`read_capped` 读时停在 256 KiB，不再 `wait_with_output` 后再 truncate。钉改名 `windows_execute_full_caps_while_reading_on_a_helper_thread`。托盘 `menu.append` / submenu append 与 macOS `menu_bar` append / Close Window accelerator 走 `append_or_warn`。钉：`tray_menu_append_does_not_unwrap`、`menu_bar_append_does_not_unwrap`（Linux include_str，无 AppKit）。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 232；fig_desktop 141 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 31（`9ba8e111`）：架构 + 正确性。WebView `WindowEvent::Reload` 删除；login/logout 只 `Show` 设置窗，不再 `location.reload` / `UpdateWindowGeometry` `SETTINGS_SIZE`。`DebugMode` 走到 overlay：`OverlayState.debug_window` 把窗口根涂红（`DEBUG_WINDOW_FILL = 0xff0000`，含透明 pad），hide/dismiss 不清。`Devtools` 在 overlay 上 `show()` 保留行（`ec debug devtools autocomplete` / 托盘 `autocomplete-devtools` 不再被 ignore）；设置窗仍把 Devtools 当 Show。`Event::ReloadTray` 去掉 `is_logged_in`：logout 不再把托盘打成「session expired / Log back in」。`get_icon()` / `get_context_menu()` / Linux GTK tray `reload()` 无 auth 分支；`LOGIN_PATH` 与 signed-out 菜单删除。hide/dismiss `sync_intercept` 走 `positioned_intercept_flags`（非 layout 时 `positioned=false`）；`intercept_flags` 是无 caret 采样的包装。钉：`overlay_handles_devtools_and_debug_mode`、`native_settings_do_not_reload_a_webview`、`tray_reload_is_not_an_auth_state`、`login_and_logout_do_not_reload_a_webview_or_fake_auth`、`tray_menu_is_not_an_auth_gate`、`intercept_flags_share_one_policy`、`debug_window_survives_hide_and_dismiss`、`debug_window_fill_is_opaque_red`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p ec_gpui -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 147 + 1 ignored；ec_gpui 100；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 32（`53687a56`）：cfg 诚实 + Linux 可钉的 Win/macOS 政策。HIToolbox `vendor_prefix` / `classify` / `scan_palette` / `rewritten_palette_ids` 进 `ec_hitoolbox::policy`（每 OS 编译）；`macos.rs` 仍 `cfg(macos)` 写 CFPreferences，list 活着时 wrap Other 条目。空 vendor prefix 仍不匹配整台机器的 palette。IME `ime_launch_action`（Start / Replace / PinHash / Keep）进 `fig_integrations::ime_launch`：缺 hash tracker **不是** stale，不会 undo `install.sh` 的 same-hash keep。`fig_input_method` 的 `terminals` / `wire` 每 OS 编译：bundle-ID 表对 `fig_util`、caret 帧对 prost、零高/非有限 rect 丢弃、重复框 coalesce（`CARET_EPS`）；`macos.rs`/`imk.rs` 仍 `cfg(macos)`，`macos_pins` include_str 钉无 `TISDisableInputSource`、无 `Command::new` palette 写。iTerm Quake 窗级 `macos_overlay_level_for_terminal(None|0 → floating, else inherit)`；NS activation Regular=0 Accessory=1 Prohibited=2 与 fullscreen/settings Accessory 规则在 `platform/caret.rs`。Win32 `win32_append_quoted`（CommandLineToArgvW）与 `win32_pathext_extension`（空/`"."` 跳过，不再 `[1..]` panic）；`CreateProcessW` include_str 钉 BOOL `if res == 0`，不喂 HRESULT mapper。`rust-linux` / `rust-windows` clippy+test 头less 列表加 `ec_hitoolbox` + `fig_input_method`。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_hitoolbox -p fig_integrations -p fig_input_method -p ec_gpui -p fig_desktop -p figterm -- -D warnings` 绿；`cargo test --locked --offline`：ec_hitoolbox 11；fig_integrations 29；fig_input_method 10；ec_gpui 101；fig_desktop 148 + 1 ignored；figterm 52 + 1 ignored + cli 1 + linux_shell_hooks 3；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/cfprefsd/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 33（`ed6151f9`）：complete/overlay/IPC 热路径。引擎 supervisor 不再每次 complete `thread::Builder` 一条 `ec-engine-attempt`：`dispatch_engine_attempt` 把 Engine 送到常驻 attempt 线程，QuickJS `INNER` TLS 跨按键活着；timeout / panic 把 sender 置 `None`，卡住的 `complete` 不能接下一个 job，下一请求再 spawn。`run_engine_attempt` 仍是单测用的一次性 spawn（watchdog isolation）。浮层 `set_intercept_flags`：`next_intercept_modes` 在 intercept 对未变时 `continue`，不发 `InterceptFigJs` / `InterceptFigJSVisible`，不 `overlay_actions()` 重读 keybindings。Hide-with-rows 仍是 Unlocked+Locked（Tab 全局拦截）。钉：`dispatch_keeps_the_attempt_sender_across_two_completes`、`supervisor_reuses_the_attempt_thread_on_the_happy_path`（spawn_supervised 无 `run_engine_attempt`，dispatch 无 `thread::Builder`）、`unchanged_intercept_modes_skip_figterm_ipc`。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 234；fig_desktop 149 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 34（`7e01a558`）：架构边界 + 命名债。`ClearAutocompleteCache` / `ResetCache` 走 `EngineClient::clear_caches`：supervisor 清 generateSpec / generator / hook source 缓存（不重建 IR index，不碰 acceptance）；drain 时 ClearCaches 与 RecordAcceptance 一样不被 latest-complete 吃掉。浮层 `clear_engine_caches` 是 `tokio::spawn` + `.await`，不 `blocking_recv` GPUI 线程。`RestartSettingsListener` 成功返回（`setup_listeners` 是进程寿命，再跑会双份 watcher）。删未编译的 `fig_integrations/src/gnome_extension.rs`（依赖不存在的 `dbus` crate）；`InstallComponents::GNOME_SHELL_EXTENSION` 与 `bundled_gnome_extension_*` 路径去掉。doctor `linux.rs` 每 OS 可编：`DisplayServerCheck` + `SandboxCheck`（假 Context 钉 x11/wayland/未知 session）；**不**编 IBus/GNOME 扩展检查（v1 补全走 PTY + 编辑缓冲，caret 走 AT-SPI/IBus，不是硬依赖）。CLI `GnomeShellExtension` 隐藏，旧脚本拿到明确错误。命名：`Event::ReloadCredentials` → `ReloadSettings`；`launch_fig_desktop` / `restart_fig` / `quit_fig` / `launch_dashboard` → desktop/settings；设置节路径 `appearance`/`behavior`/`about`（仍认旧 `/preferences` `/help`）。`ec debug devtools settings` 别名 `dashboard`；`ec://settings` 别名 `ec://dashboard`。**未改** `dashboard.language`、`--no-dashboard`（LaunchAgent）、`DumpStateWebNotifications` / `DevtoolsDashboard` proto 标签、`qterm.enabled` / `QTERM_SESSION_ID`。钉：`generate_spec_cache_clears_on_request`、`client_clear_caches_drops_generate_spec_results`、`cache_clear_jobs_survive_completion_coalescing`、`clear_autocomplete_cache_reaches_the_engine_worker`、`reset_cache_and_restart_settings_listener_are_not_unknown`、`reload_settings_is_not_an_auth_event`、`settings_section_paths_are_native_not_webview_routes`、`linux_doctor_does_not_require_a_gnome_shell_extension`、`leftover_crates` 扩 gnome_extension.rs / `GNOME_SHELL_EXTENSION`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_desktop -p fig_integrations -p fig_install -p fig_util -p ec_cli -p fig_proto -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 237；fig_desktop 154 + 1 ignored；fig_integrations 29；fig_install 15；fig_util 33 + 1 ignored；fig_proto 15；ec_cli 33 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 35（`ca161122`）：生产路径 panic 降级。fig_log `create_filter_layer` 残留的多行 `.lock().unwrap()` 改 `recover_mutex`（旧 pin 按单行字面量漏了）；`set_log_level` 在 reload handle 缺失时 `Err`，不再 expect（ecterm 日志 init 失败后仍可能收到 SetLogLevel）。桌面 / CLI 缺 HOME 或 file appender 失败 eprintln + 继续，不 panic。`backup_file` 无 file_name / `utc_backup_dir` 失败走 `io::Error`（shell/ssh 把 `.ok()` 传成 None 以前会再 unwrap 炸 install）。IME `input_method_directory` 缺 HOME 走 `InvalidDestination`。WindowsTerminal `Drop` warn，不 restore unwrap。ConPTY `kernel32` 缺导出 `open_pty` `Err`，OnceLock 替 lazy_static。钉：`log_level_locks_recover_from_poison`（compact `.lock().unwrap()`）、`desktop_logging_init_does_not_panic`、`cli_logging_init_does_not_panic`、`backup_file_does_not_unwrap`、`backup_path_without_file_name_does_not_panic`、`ime_directory_does_not_unwrap_home_dir`、`windows_terminal_drop_does_not_panic`、`conpty_create_and_resize_use_hresult_not_bool` 扩 load_conpty。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_log -p fig_desktop -p figterm -p fig_integrations -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_log 2；fig_desktop 155 + 1 ignored；figterm 53 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_integrations 34；ec_cli 34 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 50（`208b85a0`）：cfg 诚实。Windows zip/MSI 与 Linux tar.zst unpacking「未恢复」进 `fig_install::update_os_policy`（每 OS 编译）；`windows.rs` 仍 `cfg(windows)`，`linux.rs` 仍 `cfg(linux)` 写 AppImage 交换。`ec update` 非 macOS 走同一文案，不跑假更新器。Full Linux 更新仍只要 AppImage。钉：`windows_zip_msi_updater_is_not_restored`、`linux_tar_zst_unpacking_is_not_restored`、`cli_update_is_macos_only`、`live_os_updaters_stay_cfg_gated`、`leftover_crates` 扩 `update_os_policy`。**未改 framing / 无 scan-`\x1b@`。** 补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p fig_install -p ec_cli -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：fig_install 25；fig_desktop leftover_crates；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS Sparkle/DMG、未假装 Windows zip/MSI / AppImage 活更新。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 49（`b4914221`）：生产路径 panic 降级。`ec debug` logs/diagnostics/IME/shell：`ctrl_c` / glob UTF-8 / `bundle_id` / crossterm size / zshrc write / debug shell 失败走 `Result`，诊断光标插入落到 char boundary。macOS quit pid 与 `is_brew_reinstall` 静态 regex 不再 unwrap。Fake fs 未实现方法 `io::Error` 不再 `panic!("unimplemented")`；copy/rename/remove/exists 实现；mutex poison `into_inner`（与 Fake env/sysinfo 同）。Global settings 未 `init_global` 读空 map、写 `get_or_insert`，不再 `expect("global backend is not used")`。未知 `TARGET_TRIPLE` → `Other`。ecterm `Function(0)` 与嵌套 keymap `unreachable!` 发空串。钉：`debug_commands_do_not_unwrap_on_the_production_path`、`insert_cursor_mark_does_not_panic_off_a_char_boundary`、`quit_and_brew_regexes_do_not_unwrap`、`fake_fs_does_not_panic_on_unimplemented_ops`、`fake_fs_lock_recovers_from_poison`、`global_backend_does_not_panic_when_uninitialized`、`target_triple_does_not_panic_on_an_unknown_triple`、`encode_issue_892` 扩 Function(0)。**未改 framing / 无 scan-`\x1b@`。** 补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_cli -p fig_os_shim -p fig_settings -p fig_util -p figterm -- -D warnings` 绿；`cargo test --locked --offline`：fig_os_shim 29；fig_settings 20；fig_util 42 + 1 ignored；figterm 57 + 1 ignored + cli 1 + linux_shell_hooks 3；ec_cli 47 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 48（`2a8b52cb`）：架构边界 + 命名债。doctor 去掉 `#![allow(dead_code)]`：未注册的 Hyper / VSCode / iTerm / SSH / RemoteSocket / DesktopCompatibility 检查删掉（`fig-hyper-integration` / `withfig.fig` 不再被 doctor 当活路径）；`PluginDevModeCheck` 不再警告不存在的 Fig plugin 旗。macOS 安装/AX/IME 检查与 Windows console 检查 `cfg` 到对应 OS。`FigBinCheck` / `FigIntegrationsCheck` → `DataDirCheck` / `TerminalIntegrationsCheck`。未调用的 `analytics_event_name` 与仅 Hyper/VSCode 用的 `verify_integration` IPC 去掉。`pub mod internal` 改 crate-private。删未引用且会编出 `fig_test::ENVIRONMENT_LOCK` 的 `fig_test_macro` crate。Amazon Q `auth_kv` API 去掉，迁移 `006_drop_auth_table` 丢表（`005_auth_table` 留在历史）。Linux `support_level` 一律 `Supported`，不再警告 Amazon Q「other products」。**未改** `dashboard.language`、`--no-dashboard`、`DumpStateWebNotifications` / `DevtoolsDashboard` proto、`qterm.enabled` / `QTERM_SESSION_ID`、IME 通知名。钉：`leftover_crates` 扩 fig_test_macro / auth_kv / doctor allow(dead_code)+Hyper+Plugin+FigBin / `mod internal` / `verify_integration`、`amazon_q_auth_kv_is_dropped`、`linux_support_level_is_not_an_amazon_q_other_products_caveat`、`doctor_does_not_keep_webview_host_or_auth_checks` 扩。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_settings -p fig_util -p ec_cli -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：fig_settings 18；fig_util 41 + 1 ignored；fig_desktop leftover_crates + 全套 172；ec_cli product_name_tests 3 + 先前 44 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 47（`b5229cf8`）：complete/overlay 热路径。`Spec.args` / `OptionSpec.args` 改 `Vec<Arc<ArgSpec>>`（与 subcommands / options 同形）：walk 进 `git checkout` / `--opt value` 只 bump 指针，不再深拷贝 templates/scripts/suggestion seeds/JS hook 树。`ActiveArg` 与 `OptionArgState` 持 Arc。`collect_option_suggestions` / option chain 一次 `unmet_option_depends` 借 `dependsOn` `&str`，`curl -` 不再每行 HashSet+clone。`select_named_candidate` 普通路径 `.find`，不再为第一命中收集 `Vec`。浮层 paint 不 clone `SuggestionItem`：click 从 `overlay.items.get(ix)` 取行；`name_runs` fuzzy 切连续 span，不再每字符 `String` 再 merge。`set_suggestions` 只 clone 一次 identity，比较字段不每行再 `selection_identity`。**未改 framing / 无 scan-`\x1b@`。** 钉：`args_are_shared_arcs`、`listing_and_walk_do_not_deep_clone_arg_specs`、`suggestion_row_does_not_clone_the_item_on_paint`、`fuzzy_name_runs_are_consecutive_spans`、`set_suggestions_does_not_clone_row_keys_to_keep_selection`。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p ec_gpui -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 254；ec_gpui 106；fig_desktop 172；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 46（`603af387`）：生产路径 panic 降级。桌面 tokio runtime `build()` 失败 `ExitCode::FAILURE`，不再 `.expect("tokio runtime")`。`fig_util::manifest::Os::current` 未知 OS 走 `Other`，不再 `panic!("Unsupported OS")`。`partitioned_compare` 超宽数字串 lexical，不再 `u64` parse unwrap。Win32 `QueryFullProcessImageNameA` 长度越界 `None`，进 `windows_policy`（`windows.rs` 仍 `cfg(windows)`）。Fake `Env` / `SysInfo` mutex poison `into_inner`；非 UTF-8 `set_var` skip；Real `add_running_processes` 空操作。IME `MainThreadMarker` 缺则 log+return。CLI：doctor 缺 HOME / 坏 iTerm 版本 / glob / PermissionDenied 文案；`ec diagnostic` toml；spinner Drop/stop；mux websocket handshake/peer/hostbound send；`ec internal ipc` socket / IME JSON；`--format json` serialize；`match_regex` 坏模式。shell/ssh 静态 regex `?` / `filter_map`；dotfile stem `strip_prefix`。`get_max_channel` `unwrap_or(Stable)`。**未改 framing / 无 scan-`\x1b@`。** 钉：`desktop_tokio_runtime_does_not_panic`、`os_current_does_not_panic_on_an_unknown_os`、`partitioned_compare_does_not_unwrap_u64_parse`、`overflowing_reported_len_does_not_panic`、`windows_exe_lookup_uses_the_shared_bound`、`fake_env_lock_recovers_from_poison`、`fake_set_var_non_utf8_does_not_panic`、`fake_sysinfo_lock_recovers_from_poison`、`real_add_running_processes_does_not_panic`、`ime_main_does_not_unwrap_the_main_thread_marker`、`doctor_home_and_version_checks_do_not_unwrap`、`diagnostic_render_does_not_unwrap`、`spinner_stop_and_drop_do_not_unwrap`、`websocket_accept_does_not_unwrap`、`internal_ipc_and_ime_errors_do_not_unwrap`、`json_output_does_not_unwrap_serialize`、`match_regex_does_not_unwrap_a_bad_pattern`、`shell_install_regexes_do_not_unwrap`、`ssh_comment_strip_does_not_unwrap_the_regex`、`get_max_channel_does_not_unwrap_an_empty_iterator`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_util -p fig_desktop -p fig_os_shim -p fig_input_method -p fig_install -p ec_cli -p fig_integrations -- -D warnings` 绿；`cargo test --locked --offline`：fig_util 40 + 1 ignored；fig_desktop 172；fig_os_shim 27；fig_input_method 14；fig_install 21；fig_integrations 45；ec_cli 44 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept / `QueryFullProcessImageNameA`。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 45（`46c46687`）：架构边界 + 命名债。删 Amazon Q AX 查询 demo `macos-utils/accessibility-master/aq`（structopt，非 workspace member）。删 signed-out 托盘图 `not-logged-in.png` / `not-logged-in-light.png`（auth 托盘续 31 已走）。`TAURI_PRODUCT_NAME` 改走 `PRODUCT_NAME`（AppImage `lib/{PRODUCT_NAME}`，路径值未变）；删未用 `CLI_BINARY_NAME_MINIMAL`。构建元数据优先 `EC_BUILD_*`，`AMAZON_Q_BUILD_*` 仍作 fallback。`ec_cli` 去掉未引用 `arboard` / `wayland` feature / `convert_case` / `criterion`；工作区 `convert_case` 一并删；锁里 clipboard/bench 依赖图跟着走。`fig_desktop` 去掉 leftover tauri-bundler `[package.metadata.bundle]`（列出的 22x22/autocomplete-icons 本就不存在；IME crate 仍用自己的 metadata.bundle）。删 CLI `IbusBootstrap`：不再 `ibus-daemon -drxR`；IBus 仍由 `linux_caret` 宿主连。`ConnectToIbus` IPC 仍是协议错误，不 spawn。**未改** `dashboard.language`、`--no-dashboard`、`DumpStateWebNotifications` / `DevtoolsDashboard` proto、`qterm.enabled` / `QTERM_SESSION_ID`、IME 通知名。钉：`leftover_crates` 扩 aq / not-logged-in / TAURI / ec-minimal / arboard / convert_case / criterion / desktop metadata.bundle / `IbusBootstrap`、`build_env_prefers_ec_names`、`appimage_bundle_paths` 扩 PRODUCT_NAME、`connect_to_ibus_does_not_spawn_a_daemon`、`test_simplified_command_surface` 扩 `ibus-bootstrap`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_util -p fig_desktop -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_util 36 + 1 ignored；fig_desktop 171；ec_cli 37 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 44（`ee1bbccf`）：complete/overlay/IPC 热路径。filegen 按目录 mtime 缓存 listing（`ls`/`cd` 与 `src/m`/`src/ma` 共用一次 `read_dir`；`folders_only` 与 query 在缓存后过滤；缺 mtime 不缓存；32 条顶清）。`dedup_suggestions` 先借 name/kind/insert_value 再 retain，不再为 HashSet 克隆行键。浮层 `common_prefix` 在 `set_suggestions` / 选中行变化时算一次，paint 不再每帧 clone `SuggestionItem` + 走列表；`prefix_type_filter` 借 kind/name。ecterm 未变 edit-buffer 用 PathBuf 比 cwd，不再 `display().to_string()` 再跳过 protobuf。**未改 framing / 无 scan-`\x1b@`。** 钉：`complete_path_reuses_a_mtime_listing_and_filters_after_the_cache`、`directory_listing_cache_invalidates_when_the_directory_mtime_changes`、`dedup_suggestions_does_not_clone_row_keys`、`common_prefix_does_not_clone_the_selected_row`、`set_suggestions_and_selection_refresh_the_cached_common_prefix`、`send_edit_buffer_skips_duplicate_frames_before_encode` 扩 Path。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p ec_gpui -p figterm -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 252；ec_gpui 103；figterm 57 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_desktop 170；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 43（`b1716a9d`）：cfg 诚实 + Linux 可钉的 Win/macOS 政策。AX 选择长度>1 / 0×0 框不是 caret、默认宽 10px，进 `caret.rs`；`caret_position.rs` 只报 AX 调用成败。IME `deactivateServer` 后若仍是 key window 仍报 caret，进 `wire.rs`（Otty/Ghostty/Kitty 中途藏列表的洞）。桌面↔IME 通知名仍是 leftover Amazon Q 字面量，两边 include_str 钉死。Sparkle LSUIElement：scheduled 立刻弹出、自动检查开、自动下载关，进 `update_policy`（`update.rs` 仍 `cfg(macos)`）。`GetConsoleMode` BOOL 非零才成功（不再 `== 1`），成功 wrap 退出 2、失败 1；doctor 共用 mapper。`launchd_plist` 每 OS 可编；macOS 12 LaunchAgent `--is-startup --no-dashboard` / RunAtLoad / KeepAlive=false 进 `launch_at_login_policy`。DMG hash / `hdiutil` mount-point 缺则 `UpdateFailed`（不再 captures unwrap）/ 恰好一个 `.app`，进 `macos_update_policy`。钉：`macos_ax_caret_rejects_a_selection_and_a_zero_box`、`ime_caret_request_notification_is_shared_with_the_helper`、`inactive_ime_still_reports_when_the_client_is_the_key_window`、`imk_reports_caret_after_deactivate_when_the_client_is_still_key`、`lsui_element_sparkle_must_prompt_not_wait_or_silent_install`、`windows_console_wrap_uses_bool_not_eq_one`、`legacy_launch_agent_is_a_silent_startup`、`hdiutil_mount_point_reads_the_plist_string_and_does_not_panic`、`dmg_must_contain_exactly_one_app`、`leftover_crates` 扩 launchd_plist 不再 `cfg(macos)`。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p fig_input_method -p fig_util -p fig_integrations -p fig_install -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 170；fig_input_method 13；fig_util 35 + 1 ignored；fig_integrations 42；fig_install 20；ec_cli 37 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/Sparkle/cfprefsd/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept / `GetConsoleMode`。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 42（`42c357c7`）：架构 + 正确性。`ec init` Post 不再读 `shell-integrations.immediateLogin` 去注入不存在的 `ec login`，也不再每轮 prompt 摸 `desktop.auth-watcher.logged-in` / `cli.init.login-prompt.sent-at` sqlite（登录提示早就注释掉）。删 `login_prompt_code`（Amazon Q login / inline / chat / translate 文案）和注释掉的 `fig app onboarding`。`pub mod app` 改 crate-private：从未挂到 clap 的 `AppSubcommand`（Onboarding Fig ASCII、Prompts 自动更新、死 UninstallArgs）删掉，`restart_desktop` 留给 `ec restart` 和 doctor。`chat_global_context_path` / `chat_profiles_dir`（`~/.aws/amazonq`）删。桌面 `Login`/`Logout` IPC 仍只开设置窗。钉：`shell_init_does_not_inject_amazon_q_login`、`test_simplified_command_surface` 扩 `app`/`login`/`chat`/`translate`/`onboarding`、`debug_help_copy_is_native_era`、`leftover_crates` 扩 directories/init/app/`mod app`。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_util -p fig_desktop -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_util 34 + 1 ignored；fig_desktop 167；ec_cli 36 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 41（`68dff419`）：生产路径 panic 降级。IPC `inspect_frame`：长度字段不再 `try_into().expect`，`FRAME_PREFIX_LEN + body_len` 走 `checked_add`，溢出是 `BodyLengthOverflow`（与 `TryFromInt` 一样吃掉 18 字节前缀后 Err，**不是** Incomplete，不会傻等）。encode `reserve` / protobuf `with_capacity` 同样 `checked_add`。**未改 framing / 无 scan-`\x1b@`。** QuickJS `INNER` `try_borrow_mut`，嵌套 hook 返回 None 而不是 BorrowMutError。Cobra / generateSpec mtime / IR loaded LRU `remove(pos)` 走 `if let Some`。supervisor `drain_to_latest` 非 Complete 是 `continue`，不再 `unreachable!`。`ec init` 每次 prompt：`duration_since(UNIX_EPOCH)` `unwrap_or_default`；unix 上 fake `Os::Windows` 走 DontLaunch，不再 `panic!("unsupported os")`。`install.releaseUrl` / env 坏 URL 回落到默认，不再 poison `RELEASE_URL` LazyLock；`find_next_version` 缺匹配 package 是 `Ok(None)`。浮层 `previous_char_boundary` 先落到 char boundary；fuzzy `indexes.first()?`。钉：`overflowing_body_length_does_not_panic`、`inspect_frame_does_not_unwrap_the_length_field`、`nested_with_inner_does_not_panic`、`lru_touch_does_not_expect_the_position_index`、`mtime_lru_does_not_expect_the_position_index`、`loaded_spec_lru_does_not_expect_the_position_index`、supervisor `continue` 源码钉、`login_prompt_clock_does_not_unwrap`、`wrap_decision_does_not_panic_on_an_unknown_os`、`windows fake platform does not panic the wrap decision`、`invalid_release_url_falls_back_to_the_default`、`predicted_acceptance_tracks_buffer_and_cursor` 扩 mid-char。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_proto -p ec_engine -p fig_install -p ec_cli -p fig_desktop -p ec_gpui -- -D warnings` 绿；`cargo test --locked --offline`：fig_proto 17；ec_engine 249；fig_install 16；fig_ipc 21；fig_desktop 167；ec_gpui 101；ec_cli 36 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 40（`6656d9f6`）：架构边界 + 命名债。`fig_desktop` 不再链 wry/tao：几何进 `dpi.rs`（logical f64 / physical i32，无 Pixel 泛型）；`Event::ControlFlow(tao::ControlFlow)` → `Quit`；`WindowEventAll` + `SetTheme(Option<Theme>)` → `ThemeChanged`（浮层自己读 `autocomplete.theme`）；`UpdateWindowGeometry` 只剩 caret `position`（WebView size/anchor/dry-run/measure RPC 删）。ACTIVATION_POLICY 改 `MacosActivationPolicy`（`caret.rs` 已有 NS 整数映射）。删死代码 `utils::icon` 查 `fig.png`、`fig_util::search_xdg_data_dirs`、GovCloud `region_check`、未引用的 `chat_response_stubs`、未用 Amazon Q env helper（`amazon_q_sigv4` / `amazon_q_chat_shell` / `Q_CLI_CLIENT_APPLICATION` / `Q_BACKEND` / `Q_USE_SENDMESSAGE` / `Q_CUSTOM_CERT`）、工作区 `aws-smithy-*` / `aws-types`。`Q_TERM` / `Q_PARENT` / `Q_LOG_LEVEL` / `QTERM_SESSION_ID` 仍留。钉：`leftover_crates` 扩 fig_desktop Cargo.toml 无 `tao`、event 无 `WindowEventAll`/`SetTheme`/`dry_run`、无 `fig.png` / `search_xdg_data_dirs` / `region_check.rs` / `chat_response_stubs` / `amazon_q_sigv4`；`physical_to_logical_*`。Linux 上删掉的 `test_icon` ignore 不再计。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_desktop -p fig_os_shim -p fig_util -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_desktop 167；fig_os_shim 21；fig_util 34 + 1 ignored；ec_cli 34 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 39（`4c1feaba`）：complete/overlay/IPC 热路径。`Spec.options` / `persistent_options` 改 `Vec<Arc<OptionSpec>>`（与 subcommands 同形）：`gcc -` / `clang -` / `curl -` 列选项只 bump 指针，不再深拷贝上百棵带 `args` 的 option 树。`collect_option_suggestions` 排引用并在 `collect_named` 上单独算 `option_priority`，不再 `let mut option = option.clone()`。walk 的 `resolved.option` 保持引用；`passedOptions` / persistent merge 走 `share_option` / `Arc::clone`。无分隔符的 `suggestion_query_term` 借 `query`（`Cow::Borrowed`），listing 不再每行 `to_string`。ecterm：PTY 高亮多 chunk 若 buffer/cursor/cwd/coords 未变且无 pending env epoch，跳过 protobuf 编码和 socket；桌面 `edit_buffer_hook_is_duplicate` 同样不发 `GpuiOverlayBuffer`。**未改 framing / 无 scan-`\\x1b@`。** 钉：`options_are_shared_arcs`、`listing_and_walk_do_not_deep_clone_option_specs`、`suggestion_query_term_borrows_query_without_a_separator`、`unchanged_edit_buffer_frame_is_duplicate`、`send_edit_buffer_skips_duplicate_frames_before_encode`、`duplicate_edit_buffer_hook_is_detected`、`duplicate_edit_buffer_does_not_wake_the_overlay`。本机 `curl -` 仍 238 行。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全行为 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p figterm -p fig_remote_ipc -p fig_desktop -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 245；figterm 57 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_remote_ipc 5；fig_desktop 164 + 1 ignored；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 38（`0024ca05`）：cfg 诚实 + Linux 可钉的 Win/macOS 政策。`install.rs` / `macos.rs` 仍 `cfg(macos)` 写 AppKit / TCC。Accessibility 撤销只提示一次、once-per-version 安装、桌面**总是**重启 / IME 仅 hash 变才换 / `tccutil reset` 仅桌面 hash 变、`Contents/Helpers` 在 same-hash IME keep 时留下，进 `install_policy`（include_str `install.sh`）。浮层 enable（未知 bundle 关、IME 要 helper、无 restart stamp）、`CGWindowLevel` 0 才跟 AX、AX 超时 0.25s、空 screens：Linux/Windows 一律 park、macOS TopLeft（AX）仍可摆、BottomLeft（IME）无主屏高则 park，进 `caret.rs`。设置门 AX+IME 只在 macOS；`autocomplete_may_run` 去 disable + AX。IME stop `IME_SIGTERM_WAIT` 800ms 再 `IME_SIGKILL_WAIT` 400ms。`fig_util::open`：`/usr/bin/open`、`cmd /c start` + `DETACHED_PROCESS=0x8`、`xdg-open`/`wslview`，无 NSWorkspace。钉：`accessibility_revocation_prompts_once_then_falls_back_to_the_tray`、`once_per_version_install_runs_on_first_launch_and_skips_debug`、`install_sh_restarts_desktop_always_and_ime_only_on_hash_change`、`macos_overlay_enable_does_not_require_a_restart_stamp`、`macos_follows_ax_only_at_normal_window_level`、`macos_keystroke_reread_of_ax_still_respects_disable_flags`、`empty_screens_park_except_macos_top_left`、`url_openers_are_process_spawns_not_appkit`、`replace_waits_for_sigterm_then_sigkill_before_open`。补全 / caret 策略 / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_util -p fig_desktop -p fig_integrations -- -D warnings` 绿；`cargo test --locked --offline`：fig_util 34 + 1 ignored；fig_desktop 163 + 1 ignored；fig_integrations 41；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/tccutil/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept / `cmd /c start`。未 push。无单独 STATUS.md，本 §8 即状态。
- 2026-08-23 续 37（`dd9d2e9b`）：架构 + 正确性。overlay 默认快捷键 / `action_requires_visible` / figterm 隐藏态 intercept 都读 `actions.json`（`fig_settings::keybindings` 一次解析）。GPUI 口那份 `DEFAULT_OVERLAY_BINDINGS` 已偏：`selectSuggestion` 是 `command+1` 不是 `control+1`，`toggleDescription` 占了 `control+k`（JSON 里那是 `navigateUp`）。`hideAutocomplete` 仍是 Always（非激活面板），但 `intercepts_while_hidden` 把它排除，停住的浮层不偷第二次 ESC。描述提示 fallback `⌘i`。Amazon Q inline shell completion 不在编译图里：删 `figterm/src/inline/`、`ec_cli` 未挂的 `inline.rs` / `inline_shell_completion.rs`、未注入的 zsh widget 树。figterm proto 仍 no-op InlineShellCompletion。钉：`default_bindings_follow_actions_json_not_the_gpui_port_drift`、`hide_autocomplete_is_always_but_does_not_steal_esc_while_hidden`、`overlay_defaults_come_from_actions_json`、`hidden_overlay_does_not_steal_esc_or_enter`、`global_intercept_uses_actions_json_availability`、`leftover_crates` 扩 inline 路径。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p fig_settings -p fig_desktop -p figterm -p fig_integrations -p ec_cli -- -D warnings` 绿；`cargo test --locked --offline`：fig_settings 18；fig_desktop 156 + 1 ignored；figterm 55 + 1 ignored + cli 1 + linux_shell_hooks 3；fig_integrations 40；ec_cli 34 + 4 ignored + cli 4 + debug 1 + internal 9 + settings 1 + init 20 + integrations 4；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/托盘/浮层，未假装 Windows live GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 2026-08-23 续 36（`60ed15d1`）：complete 热路径 + cfg 诚实。`cached_spec` 存 `Arc<Spec>`，`apply_generate_spec` 把 merge 放进 miss 闭包：`git ch` / `git che` 命中后只 bump 指针，不再每键 clone+merge 生成树。hook JS `Arc<str>`；`generate_spec_cache_key` 走 intern 的 ignore-tokens 旗，不每键克隆/扫描 hook 源。JS generator 缓存命中 `iter`+`cloned` 只拷 query 命中行。钉：`cached_spec_returns_the_same_arc_on_hit`、`cached_suggestions_return_the_same_arc_on_hit`、`hook_source_is_shared_arc`、`generate_spec_cache_key_uses_interned_ignore_flag`、`generate_spec_always_goes_through_the_spec_cache` 扩 merge-in-miss、`js_generator_cache_hits_clone_only_query_matches`。`launch_at_login_policy` 每 OS 可编：SMAppService `from_raw` / 仅 Enabled 算已开 / RequiresApproval 仍要 register 或 unregister、两份 LaunchAgent 标签、HKCU Run 键值与 delete NotFound=ok。`login_item.rs` 仍 `cfg(macos)` 写 SMAppService；Windows `launch_at_login` 仍 `cfg(windows)` 写注册表。钉：`sm_app_service_status_matches_service_management`、`sm_app_service_skips_register_and_unregister_when_already_there`、`legacy_launch_agent_labels_cover_both_previous_install_paths`、`win32_run_key_delete_not_found_is_success`、include_str 钉 login_item / launch_at_login 走共享政策。debug CLI `git ch` 仍是新进程，不拿它当 overlay 按键数字。补全 / caret / `TERM_SCROLLBACK_LINES == 1` / 考古后端 / tokio 列表 / framing 未动。本机 Linux `cargo clippy --locked --offline -p ec_engine -p fig_integrations -- -D warnings` 绿；`cargo test --locked --offline`：ec_engine 242；fig_integrations 40；`cargo fmt --all -- --check` 绿；`ec engine complete --buffer "git ch"` 含 `checkout`。未测 macOS AX/IME/DMG/SMAppService/LaunchAgent/托盘/浮层，未假装 Windows live HKCU Run / GetGUIThreadInfo / ConPTY I/O / HWND / named-pipe accept。未 push。
- 下一步：push 后看第一次 `rust-linux` / `rust-windows`（整套 `fig_desktop` + `fig_log` + `ec_gpui` + `ec_hitoolbox` + `fig_input_method`，含 `macos_overlay` / HIToolbox policy / IME terminals+wire / `launch_at_login_policy` / `actions.json` 快捷键政策 / `install_policy` / URL-open spawn / AX caret 门 / IMK 失活仍报 / Sparkle LSUIElement / wrap BOOL / LaunchAgent plist / DMG layout / `update_os_policy` Windows zip/MSI + Linux tar.zst 未恢复）；Windows 手测 named pipe 往返、ConPTY I/O、GetGUIThreadInfo、HWND、HKCU Run、`cmd /c start`、`GetConsoleMode`。不要为了翻 skip 去给 Ubuntu 装 shellcheck 或 dbus-daemon，不要装 WebKit，不要把 `mesa-vulkan-drivers` 写进 rust-linux job，**不要复活考古 `platform/linux/` / `platform/windows.rs`**，不要把 `webview/` 目录名请回来，不要把 `fig_desktop_api` 请回来，不要把 `gnome_extension.rs` 请回来，不要把 `figterm/src/inline` / `ec_cli` inline 模块 / zsh inline_shell_completion widget 请回来，不要把 `tao` 请回 `fig_desktop`，不要把 `region_check.rs` / `chat_response_stubs` / unused Amazon Q env helpers 请回来，不要把 `AppSubcommand` / `login_prompt_code` / `chat_global_context_path` 请回来，不要把 `macos-utils/accessibility-master/aq` 请回来，不要把 `not-logged-in.png` 请回来，不要把 `TAURI_PRODUCT_NAME` / `CLI_BINARY_NAME_MINIMAL` 请回来，不要把 `arboard` / `convert_case` / `criterion` 请回 `ec_cli`，不要把 `IbusBootstrap` / CLI `ibus-daemon` spawn 请回来，不要把 tauri-bundler `[package.metadata.bundle]` 请回 `fig_desktop`。N1 / N2 / N3 / N4 / N5 已关。`match_buckets_follow_webview_exact_prefix_order` 已关。macos `focused_window.lock().unwrap()` 与 `tray.rs` `get_icon` expect 已关。fig_ipc A3 帧 resync **研究关闭，不改 framing**。macos `event_sender` / `window_server` RwLock poison、`file_watcher` `recommended_watcher` unwrap、overlay `cached_image` expect 已关。省约 R3 已关。figterm 本地 IPC send unwrap、fig_log poison、JS `executeCommand` Unix poll、macos SetEnabled / event_sender Option unwrap、用户可见 qterm copy 已关。ranking 预计算 + ASCII `match_score` + filegen sort/stat + IME/AX 政策进 caret.rs + rust-linux/windows 整套 fig_desktop/`fig_log` 已关。macos overlay 政策进 `macos_overlay` + Win32 hwndCaret / named-pipe 调用点 / ConPTY HRESULT 调用点已关。Spec 子树 Arc + ranking 优先级预计算 + settings map 不 clone + IPC 单次编码/BytesMut split 已关。`fig_desktop_api` / WebView notify bus / `FigIdMap` / `DASHBOARD_ID` / AppImage `q-desktop` 路径已关。macos `GLOBAL_PROXY.get().unwrap()` / `CString::new`（NUL）已关。token-independent generateSpec 进程内缓存已关。generateSpec **合并树** Arc 缓存 + hook JS intern + ignore-tokens intern 已关。Windows `execute_full` 读时封顶已关（仍辅助线程，不宣称 poll）。托盘 / macOS `menu_bar` append unwrap 已关。`WindowEvent::Reload` / 假 signed-out 托盘 / overlay 忽略 `DebugMode`/`Devtools` 已关。HIToolbox classify / IME launch / IMK caret 门 / NS activation / iTerm 窗级 / Win32 quoting 已关。SMAppService status / LaunchAgent 标签 / HKCU Run NotFound 已关。常驻 attempt 线程 + 未变 intercept 跳过 IPC 已关。`ClearAutocompleteCache` / `ResetCache` IPC→引擎缓存已关。`RestartSettingsListener` 进程寿命成功已关。doctor `linux.rs` GNOME/dbus 不编译已关（现编 DisplayServer + Sandbox，无 IBus 硬依赖）。fig_log `create_filter_layer` 残留 lock unwrap / `set_log_level` expect / 桌面+CLI `logs_dir` expect / `backup_file` unwrap / IME `home_dir` unwrap / WindowsTerminal Drop unwrap / ConPTY `load_conpty` expect 已关。overlay `DEFAULT_OVERLAY_BINDINGS` / figterm `GLOBAL_ACTIONS` / 未编译 Amazon Q inline 已关。install Accessibility 撤销状态机 / 桌面总重启 / IME hash 才换 / `tccutil` 仅桌面 hash / `/usr/bin/open` vs `cmd /c start` DETACHED / 空 screens BottomLeft park / 设置门 AX+IME 参数化 已关。OptionSpec Arc + listing 不深拷贝 + query_term Cow + 未变 edit-buffer 跳过 IPC 已关。wry/tao 窗口 crate / `Event::ControlFlow` / `WindowEventAll` `SetTheme` / `UpdateWindowGeometry` size-anchor-dry_run / `fig.png` XDG / unused Amazon Q env helpers / GovCloud `region_check` / `chat_response_stubs` / `aws-smithy` workspace deps 已关。IPC `inspect_frame` 长度 overflow / encode `checked_add` / QuickJS `try_borrow_mut` / LRU `remove(pos)` expect / supervisor `unreachable!` / `ec init` UNIX_EPOCH unwrap / wrap `panic!("unsupported os")` / `RELEASE_URL` LazyLock unwrap / `find_next_version` package unwrap / overlay `previous_char_boundary` 非边界切片 / fuzzy `indexes[0]` 已关。`ec init` Amazon Q login/onboarding / 未挂载的 `AppSubcommand` / `q chat` 目录 已关。AX 选择/0×0 不是 caret / IMK 失活但仍是 key window 仍报 / Sparkle LSUIElement 立刻弹出+自动检查开+自动下载关 / wrap `GetConsoleMode` BOOL 非零 / LaunchAgent `--is-startup --no-dashboard` plist 每 OS 可编 / DMG mount-point 缺则 Err 已关。filegen 目录 mtime listing 缓存 / generate `dedup_suggestions` 借行键 / overlay `common_prefix` 不每帧重算 / 未变 edit-buffer PathBuf 比 cwd 已关。ArgSpec Arc + option `dependsOn` 借一次 + listing 不收集 matching Vec + overlay paint 不 clone 行 / fuzzy span / keep-selection 不每行 clone identity 已关。Amazon Q `aq` demo / signed-out 托盘图 / `TAURI_PRODUCT_NAME` / `CLI_BINARY_NAME_MINIMAL` / 未用 `arboard` `convert_case` `criterion` / `fig_desktop` tauri-bundler metadata / CLI `IbusBootstrap` 已关（`ConnectToIbus` IPC 仍错误返回）。桌面 tokio runtime expect / `Os::current` unknown-OS panic / `partitioned_compare` u64 parse / Win32 process image `0..=len` / Fake Env+SysInfo lock unwrap / IME `MainThreadMarker` expect / CLI doctor HOME+iTerm version+glob / diagnostic `user_readable` expect / spinner Drop unwrap / mux websocket unwrap / `ec internal` socket+IME JSON expect / `--format json` serialize unwrap / `match_regex` 坏模式 / shell+ssh 静态 regex unwrap / `get_max_channel` max unwrap 已关。构建 env 优先 `EC_BUILD_*`（`AMAZON_Q_BUILD_*` fallback 仍留）。doctor `#![allow(dead_code)]` / 未注册 Hyper·VSCode·iTerm·SSH·RemoteSocket·DesktopCompatibility / `PluginDevModeCheck` / `fig_test_macro` / `auth_kv` API / Linux Amazon Q other-products caveat / unused `verify_integration` / unused `analytics_event_name` 已关。`mod internal` crate-private。不要把 `fig_test_macro` / `auth_kv` / doctor `allow(dead_code)` / `fig-hyper-integration` 检查请回来。Windows zip/MSI updater honesty / Linux tar.zst unpacking honesty / `ec update` macOS-only 已关（`update_os_policy`，`windows.rs`/`linux.rs` 仍 cfg）。debug CLI / Fake fs unimplemented / settings Global backend / `TARGET_TRIPLE` / ecterm `Function(0)` keymap panic 已关。不要把假的 Windows zip/MSI 或 Linux tar.zst 更新器请回来。`DumpStateWebNotifications` / `DevtoolsDashboard` proto 标签、`dashboard.language`、`--no-dashboard`、`qterm.enabled` / `QTERM_SESSION_ID` 仍留。桌面↔IME 通知名仍是 `com.amazon.codewhisperer.edit_buffer_updated`（两边钉死，未改字面量）。未 push。

进度勾选：

- [x] M0 文档（本文件，已经过评估修订）
- [x] PR-A1（`fig_util` Linux 常量；`fig_util` / `fig_os_shim` / `fig_proto` 在 `aarch64-unknown-linux-gnu` 上 `cargo check` 通过；macOS workspace clippy 绿）
- [x] PR-A2（`.github/workflows/ci.yml` 增加 `rust-linux`；不装 WebKit；首次 GitHub 原生 run 仍待推送后验证）
- [x] PR-A3（切断 `dbus` 编译图；`ec engine complete --buffer "git ch"` 本机成功含 `checkout`；Linux CI 扩到 `ec_cli`）
- [x] PR-B1（`fig_ipc` Linux 目标 `cargo check` 过；`figterm` scrollback 钉死为 1、tokio 2 worker 未改；socket 路径测试 + Linux CI 扩到 `fig_ipc`/`figterm`）
- [x] PR-B2（无桌面 `ec init` 与 macOS 同策略；zsh hook 在临时 HOME 可装可卸；IME 不进入 Linux `integrations install all`）
- [x] PR-C1（`ec_gpui` 的 AppKit 模块 `cfg(macos)`；非 Mac 走 `platform_stub`，`overlay_screens` 为空所以无 caret 则无法摆放；本机交叉编译卡在缺 linux gcc，Ubuntu 原生 gpui 仍待 CI）
- [x] PR-C2（`fig_desktop` 活路是 macos / `linux_caret` / `windows_caret`；`platform/stub.rs` 只给其它 OS。无 caret 则 park；考古 `linux/` 与 `windows.rs` **已删除，勿恢复**；GNOME/IBus 安装路径切断；Linux CI 扩到 `ec_gpui`/`fig_desktop` clippy，系统依赖是 GTK/X11/Vulkan **不含 WebKit**。Ubuntu 原生 gpui 首次绿灯仍待推送后验证）
- [x] PR-D1（新建 `platform/linux_caret/`：X11 焦点跟踪 + zbus IBus；几何换算在 `platform/caret.rs` 单测钉死；无 caret 不摆放；旧 `platform/linux/` **已删除，勿恢复**）
- [x] PR-D2（GPUI 0.2.2 无 layer-shell，浮层仍走 X11/XWayland。GNOME Wayland 终端 caret 走 AT-SPI `GetCharacterExtents(SCREEN)`，不用 Shell 扩展；窗口 `GetExtents` 只给 IBus relative 当原点，不当列表位置。无 a11y 总线或非终端 focus 则隐藏）
- [x] PR-D3（`ec_gpui/src/linux.rs`：按标题找 overlay，`unmap` park、`configure`+`map` 显示；启动时若有 `DISPLAY` 则清掉 `WAYLAND_DISPLAY` 让 GPUI 走 X11，`EC_GPUI_BACKEND=wayland` 可退出；无屏幕列表则 park，不用窗口矩形当 edges。`linux_overlay` 钉死 NOTIFICATION + ABOVE、不发 `_NET_ACTIVE_WINDOW`；map 后再 ClientMessage。本机 xfwm4 手测浮层不是 `_NET_ACTIVE_WINDOW`。）
- [x] PR-E1 / PR-E2（`scripts/build-linux.sh` 前缀布局 + tar.gz；`scripts/install-linux.sh --prefix`；`.desktop` + hicolor 图标；不装 WebKit，不改 `build-app.sh`）
- [ ] 阶段 F（进行中。F1–F6 代码在树：slug / `~user` / BOOL+HRESULT / caret 换算 / SetWindowPos 策略 / zip 布局可在 Linux 单测。Live named-pipe accept、ConPTY I/O、GetGUIThreadInfo、HWND 仍要 Windows 主机。CI 待 push。）
