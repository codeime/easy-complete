[toolchain]
channel = "1.88.0"
profile = "minimal"
components = ["rustfmt", "clippy", "rust-analyzer"]
# Host triple only. Extra `targets` would make Linux/Windows CI download
# Darwin std (and the reverse on macOS) for no compile benefit.
